//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Collections.Specialized;
using System.Linq;
using System.Web.Mvc;
using System.ComponentModel.DataAnnotations;
using System.ServiceModel;
using Dwp.Adep.Ucb.ResourceLibrary;
using AutoMapper;
using UcbWeb.Helpers;
using UcbWeb.ViewModels;
using UcbWeb.Models;
using UcbWeb.UcbService;


namespace UcbWeb.Controllers
{
    public partial class IncidentUpdateEventController : BaseController
    {

		private IUcbService UcbService;

		// Dependency Injection enabled constructors
        public IncidentUpdateEventController()
            : this(new UcbServiceClient(),new SessionManager(), new CacheManager())
        {
        }

        public IncidentUpdateEventController(IUcbService UcbService, ISessionManager sessionManager, ICacheManager cacheManager)
			:base(sessionManager,cacheManager)
        {
            this.UcbService = UcbService;
        }

		#region Edit
		
        // GET: /IncidentUpdateEvent/Edit
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        public ActionResult Edit()
        {
			// Retrieve ID from session
			string code = sessionManager.IncidentUpdateEventCode;
			
            IncidentUpdateEventVM model = new IncidentUpdateEventVM();
			
            // Not from staff or error
            if (String.IsNullOrEmpty(code))
            {
                //If session has lists then use them
                RepopulateListsFromCacheSession(model);

                //Assume we are in create mode as no code passed
                model.IncidentUpdateEventItem = new IncidentUpdateEventModel();
            }
            //if we have been passed a code then assume we are in edit situation and we need to retrieve from the database.
            else
            {
				// Create service instance
				IUcbService sc = UcbService;
				
                try
                {
					// Call service to get IncidentUpdateEvent item and any associated lookups    
                    IncidentUpdateEventVMDC returnedObject = sc.GetIncidentUpdateEvent(CurrentUser, CurrentUser, appID, "", code);

					// Close service communication
					((ICommunicationObject)sc).Close();

                    //Get view model from service
                    model = ConvertIncidentUpdateEventDC(returnedObject);

                    ResolveFieldCodesToFieldNamesUsingLists(model);

                    //Store the service version
                    sessionManager.IncidentUpdateEventServiceVersion = model.IncidentUpdateEventItem;
                }
				catch (Exception e)
				{
					// Handle the exception
					string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
					model.Message = message;
					
					return View(model);
				}
            }

            //Adds current retrieved IncidentUpdateEvent to session
            sessionManager.CurrentIncidentUpdateEvent = model.IncidentUpdateEventItem;
            SetAccessContext(model);
            
            return View(model);
        }    
		
		#endregion
		
		#region Create/Update

        // POST: /IncidentUpdateEvent/Edit with Create button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult CreateIncidentUpdateEvent(FormCollection collection)
        {
            return UpdateIncidentUpdateEvent();
        }

        // POST: /IncidentUpdateEvent/Edit with Save button submitting
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult SaveIncidentUpdateEvent(FormCollection collection)
        {
            return UpdateIncidentUpdateEvent();
        }

        //This method is shared between create and save
        private ActionResult UpdateIncidentUpdateEvent()
        {
			// Get the updated model
            var model = GetUpdatedModel();

			// Test to see if there are any errors
            var errors = ModelState
                    .Where(x => x.Value.Errors.Count > 0)
                    .Select(x => new { x.Key, x.Value.Errors[0].ErrorMessage })
                    .ToArray();
            
			//Set flags false
            SetFlagsFalse(model);
			
			// Test to see if the model has validated correctly
            if (ModelState.IsValid)
            {
				// Create service instance
				IUcbService sc = UcbService;
				
                //Attempt update
                try
                {
                    // Map model to data contract
                    IncidentUpdateEventDC IncidentUpdateEventItem = Mapper.Map<IncidentUpdateEventDC>(model.IncidentUpdateEventItem);

					IncidentUpdateEventVMDC returnedObject = null;

                    if (null == model.IncidentUpdateEventItem.Code || model.IncidentUpdateEventItem.Code == Guid.Empty)
                    {
						// Call service to create new IncidentUpdateEvent item
                        returnedObject = sc.CreateIncidentUpdateEvent(CurrentUser, CurrentUser, appID, "", IncidentUpdateEventItem);
                    }
                    else
                    {
						// Call service to update IncidentUpdateEvent item
                        returnedObject = sc.UpdateIncidentUpdateEvent(CurrentUser, CurrentUser, appID, "", IncidentUpdateEventItem);
                    }

					// Close service communication
					((ICommunicationObject)sc).Close();
					
					// Retrieve item returned by service
                    var createdIncidentUpdateEvent = returnedObject.IncidentUpdateEventItem;

					// Map data contract to model
                    model.IncidentUpdateEventItem = Mapper.Map<IncidentUpdateEventModel>(createdIncidentUpdateEvent);
					
                    //After creation some of the fields are display only so we need the resolved look up nmames
                    ResolveFieldCodesToFieldNamesUsingLists(model);

					// Set access context to Edit mode
                    model.AccessContext = IncidentUpdateEventAccessContext.Edit;

					// Save version of item returned by service into session
                    sessionManager.IncidentUpdateEventServiceVersion = model.IncidentUpdateEventItem;
                    sessionManager.CurrentIncidentUpdateEvent = model.IncidentUpdateEventItem;
                    
                    // Remove the state from the model as these are being populated by the controller and the HTML helpers are being populated with
					// the POSTED values and not the changed ones.
                    ModelState.Clear();
                    model.Message = Resources.MESSAGE_UPDATE_SUCCEEDED;
                }
				catch (Exception e)
				{
					// Handle the exception
					string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
					model.Message = message;
					
					return View(model);
				}
            }

            return View(model);
        }

        #endregion
		
		#region Exit

        // POST: /IncidentUpdateEvent/Edit with Exit button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult ExitIncidentUpdateEvent(FormCollection collection)
        {
            var model = GetUpdatedModel();
            if (model.IsExitConfirmed == "True" || !model.IsViewDirty)
            {
                //Set flags false
                SetFlagsFalse(model);

                model.IsExitConfirmed = "False";
                
                //remove the current values from session
                sessionManager.CurrentIncidentUpdateEvent = null;
                sessionManager.IncidentUpdateEventServiceVersion = null;

                return RedirectToAction("Search", "IncidentUpdateEvent");
                
            }
            else
            {
                //Set flags false
                SetFlagsFalse(model);
                model.Message = Resources.MESSAGE_EXITCONFIRMATION;
                model.IsExitConfirmed = "True";
            }
            
            return View(model);
        }
		
		#endregion
		
		#region Delete

        // POST: /IncidentUpdateEvent/Edit with Delete button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult DeleteIncidentUpdateEvent(FormCollection collection)
        {
            var model = GetUpdatedModel();
            if (model.IsDeleteConfirmed == "True")
            {
                //Set flags false
                SetFlagsFalse(model);
            
                model.IsDeleteConfirmed = "False";
				
				// Create service instance
				IUcbService sc = UcbService;
				
                try
                {
					// Call service to delete the item
                    sc.DeleteIncidentUpdateEvent(CurrentUser, CurrentUser, appID, "", model.IncidentUpdateEventItem.Code.ToString(), model.IncidentUpdateEventItem.RowIdentifier.ToString());

					// Close service communication
					((ICommunicationObject)sc).Close();
					
                    // Remove the current values from session
                    sessionManager.CurrentIncidentUpdateEvent = null;
                    sessionManager.IncidentUpdateEventServiceVersion = null;

                    // Remove the state from the model as these are being populated by the controller and the HTML helpers are being populated with
					// the POSTED values and not the changed ones.
                    ModelState.Clear();
					
					// Create new item but keep any lists
					model.IncidentUpdateEventItem = new IncidentUpdateEventModel();
					
					// Set message to return to user
                    model.Message = Resources.MESSAGE_DELETE_SUCCEEDED;
					
					// Set access context to Edit mode
                    model.AccessContext = IncidentUpdateEventAccessContext.Create;
					
					// Redirect to the search screen
                    return View(model);
                }
				catch (Exception e)
                {
					// Handle the exception
					string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
					model.Message = message;
					
					return View(model);
                }
            }
            else
            {
                //Set flags false
                SetFlagsFalse(model);
                model.Message = Resources.MESSAGE_DELETECONFIRMATION;
                model.IsDeleteConfirmed = "True";
            }
            
            return View(model);
        }
		
		#endregion
		
		#region New

        // POST: /IncidentUpdateEvent/Edit with New button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamSearch(Prefix = "Search::")]
        [HttpPost]
        public ActionResult NewIncidentUpdateEvent(FormCollection collection)
        {
            var model = GetUpdatedModel();
            //Set flags false
            SetFlagsFalse(model);
            
            //Clear Down Session
			sessionManager.IncidentUpdateEventCode = null;
            sessionManager.CurrentIncidentUpdateEvent = null;
            sessionManager.IncidentUpdateEventServiceVersion = null;
            
            //Go to the Edit Screen
            return RedirectToAction("Edit", "IncidentUpdateEvent");
        }
		
		#endregion

		#region Search

        // GET: /IncidentUpdateEvent/Search
        //This is called when first entering search IncidentUpdateEvent screen or when paging
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        public ActionResult Search(int page = 1)
        {   
			// Create service instance
			IUcbService sc = UcbService;
			
			// Create model
			IncidentUpdateEventSearchVM model = new IncidentUpdateEventSearchVM();
			
			try
			{
				IncidentUpdateEventSearchVMDC response = sc.SearchIncidentUpdateEvent(CurrentUser, CurrentUser, appID, "", null, page, PageSize);

				// Close service communication
				((ICommunicationObject)sc).Close();

				//Map response back to view model
				model.MatchList = Mapper.Map<IEnumerable<IncidentUpdateEventSearchMatchDC>, List<IncidentUpdateEventSearchMatchModel>>(response.MatchList);

				// Set paging values
				model.TotalRows = response.RecordCount;
				model.PageSize = sessionManager.PageSize;
				model.PageNumber = page;
				
				// Store the page number we were on
				sessionManager.IncidentUpdateEventPageNumber = model.PageNumber;
	        
				return View(model);
			}
			catch (Exception e)
			{
				// Handle the exception
				string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
				model.Message = message;
				
				return View(model);
			}

        }

        // POST: /IncidentUpdateEvent/Search
        //This is called when clicking search button
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
		[HttpParamSearch(Prefix = "Search::")]
        [HttpPost]
        public ActionResult SearchPost(IncidentUpdateEventSearchVM model, int page = 1)
        {
		
			// Iterate through form keys
            foreach (string Key in Request.Form.Keys)
            {
				// Test if Select button was clicked...
                if (Key.StartsWith("Search::SearchPost_"))
                {
					// Retrieve ID for entity which was selected
                    Guid Value = Guid.Parse(Key.Substring(19));
									
					// Store ID for Edit screen
					sessionManager.IncidentUpdateEventCode = Value.ToString();
					
					// Call out to Edit screen
                    return RedirectToAction("Edit", "IncidentUpdateEvent", new { code = Value });

                }
            }

            // Return to the Screen
            return View(model);
        }

        #endregion
		
		#region Private methods
		
        private void SetFlagsFalse(IncidentUpdateEventVM model)
        {
            model.IsDeleteConfirmed = "False";
            model.IsExitConfirmed = "False";
            model.IsNewConfirmed = "False";

            //Stop the binder resetting the posted values
            ModelState.Remove("IsDeleteConfirmed");
            ModelState.Remove("IsExitConfirmed");
            ModelState.Remove("IsNewConfirmed");
        }

        private void ResolveFieldCodesToFieldNamesUsingLists(IncidentUpdateEventVM model)
        {
			//TODO:
        }

        /// <summary>
        /// Private method to merge in the model 
        /// </summary>
        /// <returns></returns>
        private IncidentUpdateEventVM GetUpdatedModel()
        {
            IncidentUpdateEventVM model = new IncidentUpdateEventVM();
            RepopulateListsFromCacheSession(model);
            model.Message = "";

            if (sessionManager.CurrentIncidentUpdateEvent != null)
            {
                model.IncidentUpdateEventItem = sessionManager.CurrentIncidentUpdateEvent;
            }

            //***************************************NEED WHITE LIST ---- BLACK LIST ------ TO PREVENT OVERPOSTING **************************
            bool result = TryUpdateModel(model);//This also validates and sets ModelState
            //*******************************************************************************************************************************
            if (sessionManager.CurrentIncidentUpdateEvent != null)
            {
                //*****************************************PREVENT OVER POSTING ATTACKS******************************************************
                //Get the values for read only fields from session
                MergeNewValuesWithOriginal(model.IncidentUpdateEventItem);
                //***************************************************************************************************************************
            }

            SetAccessContext(model);

            return model;
        }
		
        private IncidentUpdateEventVM ConvertIncidentUpdateEventDC(IncidentUpdateEventVMDC returnedObject)
        {
            IncidentUpdateEventVM model = new IncidentUpdateEventVM();
            
			// Map IncidentUpdateEvent Item
			model.IncidentUpdateEventItem = Mapper.Map<IncidentUpdateEventDC, IncidentUpdateEventModel>(returnedObject.IncidentUpdateEventItem);
            
			// Map lookup data lists
			model.IncidentList = Mapper.Map<IEnumerable<IncidentDC>, List<IncidentModel>>(returnedObject.IncidentList);
        
            return model;
        }
		
		private void RepopulateListsFromCacheSession(IncidentUpdateEventVM model)
        {
			// Populate cached lists if they are empty. Will invoke service call
            IncidentUpdateEventLookupListsCacheObject CachedLists = cacheManager.IncidentUpdateEventListCache;

			// Retrieve any cached lists to model
			model.IncidentList = CachedLists.IncidentList;
   
        }

        private void MergeNewValuesWithOriginal(IncidentUpdateEventModel modelFromView)
        {
            //***************************The values that are display only will not be posted back so need to get them from session**************************

            IncidentUpdateEventModel OriginalValuesFromSession = sessionManager.CurrentIncidentUpdateEvent;

        }
		
		private void SetAccessContext(IncidentUpdateEventVM model)
        {
            //Decide on access context
            if (null == model.IncidentUpdateEventItem || model.IncidentUpdateEventItem.Code == Guid.Empty)
            {
				// Create context
                model.AccessContext = IncidentUpdateEventAccessContext.Create;
            }
            else
            {
				// Edit context
                model.AccessContext = IncidentUpdateEventAccessContext.Edit;
            }
        }
		
		private void DetermineIsDirty(IncidentUpdateEventVM model)
        {
            //Compare the IncidentUpdateEvent to the original session
            if (model.IncidentUpdateEventItem.PublicInstancePropertiesEqual(sessionManager.IncidentUpdateEventServiceVersion, "RowIdentifier"))
            {
                model.IsViewDirty = false;
            }
            else
            {
                model.IsViewDirty = true;
            }

        }
        #endregion
       
    }
}
