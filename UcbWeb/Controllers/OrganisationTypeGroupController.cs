//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Collections.Specialized;
using System.Linq;
using System.Web.Mvc;
using System.ComponentModel.DataAnnotations;
using System.ServiceModel;
using Dwp.Adep.Ucb.ResourceLibrary;
using AutoMapper;
using UcbWeb.Helpers;
using UcbWeb.ViewModels;
using UcbWeb.Models;
using UcbWeb.UcbService;


namespace UcbWeb.Controllers
{
    public partial class OrganisationTypeGroupController : BaseController
    {

		private IUcbService UcbService;

		// Dependency Injection enabled constructors
        public OrganisationTypeGroupController()
            : this(new UcbServiceClient(),new SessionManager(), new CacheManager())
        {
        }

        public OrganisationTypeGroupController(IUcbService UcbService, ISessionManager sessionManager, ICacheManager cacheManager)
			:base(sessionManager,cacheManager)
        {
            this.UcbService = UcbService;
        }

		#region Edit
		
        // GET: /OrganisationTypeGroup/Edit
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        public ActionResult Edit()
        {
			// Retrieve ID from session
			string code = sessionManager.OrganisationTypeGroupCode;
			
            OrganisationTypeGroupVM model = new OrganisationTypeGroupVM();
			
            // Not from staff or error
            if (String.IsNullOrEmpty(code))
            {
                //If session has lists then use them
                RepopulateListsFromCacheSession(model);

                //Assume we are in create mode as no code passed
                model.OrganisationTypeGroupItem = new OrganisationTypeGroupModel(){IsActive = true};
            }
            //if we have been passed a code then assume we are in edit situation and we need to retrieve from the database.
            else
            {
				// Create service instance
				IUcbService sc = UcbService;
				
                try
                {
					// Call service to get OrganisationTypeGroup item and any associated lookups    
                    OrganisationTypeGroupVMDC returnedObject = sc.GetOrganisationTypeGroup(CurrentUser, CurrentUser, appID, "", code);

					// Close service communication
					((ICommunicationObject)sc).Close();

                    //Get view model from service
                    model = ConvertOrganisationTypeGroupDC(returnedObject);

                    ResolveFieldCodesToFieldNamesUsingLists(model);

                    //Store the service version
                    sessionManager.OrganisationTypeGroupServiceVersion = model.OrganisationTypeGroupItem;
                }
				catch (Exception e)
				{
					// Handle the exception
					string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
					model.Message = message;
					
					return View(model);
				}
            }

            //Adds current retrieved OrganisationTypeGroup to session
            sessionManager.CurrentOrganisationTypeGroup = model.OrganisationTypeGroupItem;
            SetAccessContext(model);
            
            return View(model);
        }    
		
		#endregion
		
		#region Create/Update

        // POST: /OrganisationTypeGroup/Edit with Create button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult CreateOrganisationTypeGroup(FormCollection collection)
        {
            return UpdateOrganisationTypeGroup();
        }

        // POST: /OrganisationTypeGroup/Edit with Save button submitting
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult SaveOrganisationTypeGroup(FormCollection collection)
        {
            return UpdateOrganisationTypeGroup();
        }

        //This method is shared between create and save
        private ActionResult UpdateOrganisationTypeGroup()
        {
			// Get the updated model
            var model = GetUpdatedModel();

			// Test to see if there are any errors
            var errors = ModelState
                    .Where(x => x.Value.Errors.Count > 0)
                    .Select(x => new { x.Key, x.Value.Errors[0].ErrorMessage })
                    .ToArray();
            
			//Set flags false
            SetFlagsFalse(model);
			
			// Test to see if the model has validated correctly
            if (ModelState.IsValid)
            {
				// Create service instance
				IUcbService sc = UcbService;
				
                //Attempt update
                try
                {
                    // Map model to data contract
                    OrganisationTypeGroupDC OrganisationTypeGroupItem = Mapper.Map<OrganisationTypeGroupDC>(model.OrganisationTypeGroupItem);

					OrganisationTypeGroupVMDC returnedObject = null;

                    if (null == model.OrganisationTypeGroupItem.Code || model.OrganisationTypeGroupItem.Code == Guid.Empty)
                    {
						// Call service to create new OrganisationTypeGroup item
                        returnedObject = sc.CreateOrganisationTypeGroup(CurrentUser, CurrentUser, appID, "", OrganisationTypeGroupItem);
                    }
                    else
                    {
						// Call service to update OrganisationTypeGroup item
                        returnedObject = sc.UpdateOrganisationTypeGroup(CurrentUser, CurrentUser, appID, "", OrganisationTypeGroupItem);
                    }

					// Close service communication
					((ICommunicationObject)sc).Close();
					
					// Retrieve item returned by service
                    var createdOrganisationTypeGroup = returnedObject.OrganisationTypeGroupItem;

					// Map data contract to model
                    model.OrganisationTypeGroupItem = Mapper.Map<OrganisationTypeGroupModel>(createdOrganisationTypeGroup);
					
                    //After creation some of the fields are display only so we need the resolved look up nmames
                    ResolveFieldCodesToFieldNamesUsingLists(model);

					// Set access context to Edit mode
                    model.AccessContext = OrganisationTypeGroupAccessContext.Edit;

					// Save version of item returned by service into session
                    sessionManager.OrganisationTypeGroupServiceVersion = model.OrganisationTypeGroupItem;
                    sessionManager.CurrentOrganisationTypeGroup = model.OrganisationTypeGroupItem;
                    
                    // Remove the state from the model as these are being populated by the controller and the HTML helpers are being populated with
					// the POSTED values and not the changed ones.
                    ModelState.Clear();
                    model.Message = Resources.MESSAGE_UPDATE_SUCCEEDED;
                }
				catch (Exception e)
				{
					// Handle the exception
					string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
					model.Message = message;
					
					return View(model);
				}
            }

            return View(model);
        }

        #endregion
		
		#region Exit

        // POST: /OrganisationTypeGroup/Edit with Exit button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult ExitOrganisationTypeGroup(FormCollection collection)
        {
            var model = GetUpdatedModel();
            if (model.IsExitConfirmed == "True" || !model.IsViewDirty)
            {
                //Set flags false
                SetFlagsFalse(model);

                model.IsExitConfirmed = "False";
                
                //remove the current values from session
                sessionManager.CurrentOrganisationTypeGroup = null;
                sessionManager.OrganisationTypeGroupServiceVersion = null;

                return RedirectToAction("Search", "OrganisationTypeGroup");
                
            }
            else
            {
                //Set flags false
                SetFlagsFalse(model);
                model.Message = Resources.MESSAGE_EXITCONFIRMATION;
                model.IsExitConfirmed = "True";
            }
            
            return View(model);
        }
		
		#endregion
		
		#region Delete

        // POST: /OrganisationTypeGroup/Edit with Delete button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult DeleteOrganisationTypeGroup(FormCollection collection)
        {
            var model = GetUpdatedModel();
            if (model.IsDeleteConfirmed == "True")
            {
                //Set flags false
                SetFlagsFalse(model);
            
                model.IsDeleteConfirmed = "False";
				
				// Create service instance
				IUcbService sc = UcbService;
				
                try
                {
					// Call service to delete the item
                    sc.DeleteOrganisationTypeGroup(CurrentUser, CurrentUser, appID, "", model.OrganisationTypeGroupItem.Code.ToString(), model.OrganisationTypeGroupItem.RowIdentifier.ToString());

					// Close service communication
					((ICommunicationObject)sc).Close();
					
                    // Remove the current values from session
                    sessionManager.CurrentOrganisationTypeGroup = null;
                    sessionManager.OrganisationTypeGroupServiceVersion = null;

                    // Remove the state from the model as these are being populated by the controller and the HTML helpers are being populated with
					// the POSTED values and not the changed ones.
                    ModelState.Clear();
					
					// Create new item but keep any lists
					model.OrganisationTypeGroupItem = new OrganisationTypeGroupModel(){IsActive = true};
					
					// Set message to return to user
                    model.Message = Resources.MESSAGE_DELETE_SUCCEEDED;
					
					// Set access context to Edit mode
                    model.AccessContext = OrganisationTypeGroupAccessContext.Create;
					
					// Redirect to the search screen
                    return View(model);
                }
				catch (Exception e)
                {
					// Handle the exception
					string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
					model.Message = message;
					
					return View(model);
                }
            }
            else
            {
                //Set flags false
                SetFlagsFalse(model);
                model.Message = Resources.MESSAGE_DELETECONFIRMATION;
                model.IsDeleteConfirmed = "True";
            }
            
            return View(model);
        }
		
		#endregion
		
		#region New

        // POST: /OrganisationTypeGroup/Edit with New button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamSearch(Prefix = "Search::")]
        [HttpPost]
        public ActionResult NewOrganisationTypeGroup(FormCollection collection)
        {
            var model = GetUpdatedModel();
            //Set flags false
            SetFlagsFalse(model);
            
            //Clear Down Session
			sessionManager.OrganisationTypeGroupCode = null;
            sessionManager.CurrentOrganisationTypeGroup = null;
            sessionManager.OrganisationTypeGroupServiceVersion = null;
            
            //Go to the Edit Screen
            return RedirectToAction("Edit", "OrganisationTypeGroup");
        }
		
		#endregion

		#region Search

        // GET: /OrganisationTypeGroup/Search
        //This is called when first entering search OrganisationTypeGroup screen or when paging
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        public ActionResult Search(int page = 1)
        {   
			// Create service instance
			IUcbService sc = UcbService;
			
			// Create model
			OrganisationTypeGroupSearchVM model = new OrganisationTypeGroupSearchVM();
			
			try
			{
				OrganisationTypeGroupSearchVMDC response = sc.SearchOrganisationTypeGroup(CurrentUser, CurrentUser, appID, "", null, page, PageSize, true);

				// Close service communication
				((ICommunicationObject)sc).Close();

				//Map response back to view model
				model.MatchList = Mapper.Map<IEnumerable<OrganisationTypeGroupSearchMatchDC>, List<OrganisationTypeGroupSearchMatchModel>>(response.MatchList);

				// Set paging values
				model.TotalRows = response.RecordCount;
				model.PageSize = sessionManager.PageSize;
				model.PageNumber = page;
				
				// Store the page number we were on
				sessionManager.OrganisationTypeGroupPageNumber = model.PageNumber;
	        
				return View(model);
			}
			catch (Exception e)
			{
				// Handle the exception
				string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
				model.Message = message;
				
				return View(model);
			}

        }

        // POST: /OrganisationTypeGroup/Search
        //This is called when clicking search button
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
		[HttpParamSearch(Prefix = "Search::")]
        [HttpPost]
        public ActionResult SearchPost(OrganisationTypeGroupSearchVM model, int page = 1)
        {
		
			// Iterate through form keys
            foreach (string Key in Request.Form.Keys)
            {
				// Test if Select button was clicked...
                if (Key.StartsWith("Search::SearchPost_"))
                {
					// Retrieve ID for entity which was selected
                    Guid Value = Guid.Parse(Key.Substring(19));
									
					// Store ID for Edit screen
					sessionManager.OrganisationTypeGroupCode = Value.ToString();
					
					// Call out to Edit screen
                    return RedirectToAction("Edit", "OrganisationTypeGroup", new { code = Value });

                }
            }

            // Return to the Screen
            return View(model);
        }

        #endregion
		
		#region Private methods
		
        private void SetFlagsFalse(OrganisationTypeGroupVM model)
        {
            model.IsDeleteConfirmed = "False";
            model.IsExitConfirmed = "False";
            model.IsNewConfirmed = "False";

            //Stop the binder resetting the posted values
            ModelState.Remove("IsDeleteConfirmed");
            ModelState.Remove("IsExitConfirmed");
            ModelState.Remove("IsNewConfirmed");
        }

        private void ResolveFieldCodesToFieldNamesUsingLists(OrganisationTypeGroupVM model)
        {
			//TODO:
        }

        /// <summary>
        /// Private method to merge in the model 
        /// </summary>
        /// <returns></returns>
        private OrganisationTypeGroupVM GetUpdatedModel()
        {
            OrganisationTypeGroupVM model = new OrganisationTypeGroupVM();
            RepopulateListsFromCacheSession(model);
            model.Message = "";

            if (sessionManager.CurrentOrganisationTypeGroup != null)
            {
                model.OrganisationTypeGroupItem = sessionManager.CurrentOrganisationTypeGroup;
            }

            //***************************************NEED WHITE LIST ---- BLACK LIST ------ TO PREVENT OVERPOSTING **************************
            bool result = TryUpdateModel(model);//This also validates and sets ModelState
            //*******************************************************************************************************************************
            if (sessionManager.CurrentOrganisationTypeGroup != null)
            {
                //*****************************************PREVENT OVER POSTING ATTACKS******************************************************
                //Get the values for read only fields from session
                MergeNewValuesWithOriginal(model.OrganisationTypeGroupItem);
                //***************************************************************************************************************************
            }

            SetAccessContext(model);

            return model;
        }
		
        private OrganisationTypeGroupVM ConvertOrganisationTypeGroupDC(OrganisationTypeGroupVMDC returnedObject)
        {
            OrganisationTypeGroupVM model = new OrganisationTypeGroupVM();
            
			// Map OrganisationTypeGroup Item
			model.OrganisationTypeGroupItem = Mapper.Map<OrganisationTypeGroupDC, OrganisationTypeGroupModel>(returnedObject.OrganisationTypeGroupItem);
            
			// Map lookup data lists
        
            return model;
        }
		
		private void RepopulateListsFromCacheSession(OrganisationTypeGroupVM model)
        {
			// Populate cached lists if they are empty. Will invoke service call
            OrganisationTypeGroupLookupListsCacheObject CachedLists = cacheManager.OrganisationTypeGroupListCache;

			// Retrieve any cached lists to model
   
        }

        private void MergeNewValuesWithOriginal(OrganisationTypeGroupModel modelFromView)
        {
            //***************************The values that are display only will not be posted back so need to get them from session**************************

            OrganisationTypeGroupModel OriginalValuesFromSession = sessionManager.CurrentOrganisationTypeGroup;

        }
		
		private void SetAccessContext(OrganisationTypeGroupVM model)
        {
            //Decide on access context
            if (null == model.OrganisationTypeGroupItem || model.OrganisationTypeGroupItem.Code == Guid.Empty)
            {
				// Create context
                model.AccessContext = OrganisationTypeGroupAccessContext.Create;
            }
            else
            {
				// Edit context
                model.AccessContext = OrganisationTypeGroupAccessContext.Edit;
            }
        }
		
		private void DetermineIsDirty(OrganisationTypeGroupVM model)
        {
            //Compare the OrganisationTypeGroup to the original session
            if (model.OrganisationTypeGroupItem.PublicInstancePropertiesEqual(sessionManager.OrganisationTypeGroupServiceVersion, "RowIdentifier"))
            {
                model.IsViewDirty = false;
            }
            else
            {
                model.IsViewDirty = true;
            }

        }
        #endregion
       
    }
}
