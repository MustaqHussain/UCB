//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Collections.Specialized;
using System.Linq;
using System.Web.Mvc;
using System.ComponentModel.DataAnnotations;
using System.ServiceModel;
using Dwp.Adep.Ucb.ResourceLibrary;
using AutoMapper;
using UcbWeb.Helpers;
using UcbWeb.ViewModels;
using UcbWeb.Models;
using UcbWeb.UcbService;


namespace UcbWeb.Controllers
{
    public partial class ApplicationOrganisationTypeGroupController : BaseController
    {

		private IUcbService UcbService;

		// Dependency Injection enabled constructors
        public ApplicationOrganisationTypeGroupController()
            : this(new UcbServiceClient(),new SessionManager(), new CacheManager())
        {
        }

        public ApplicationOrganisationTypeGroupController(IUcbService UcbService, ISessionManager sessionManager, ICacheManager cacheManager)
			:base(sessionManager,cacheManager)
        {
            this.UcbService = UcbService;
        }

		#region Edit
		
        // GET: /ApplicationOrganisationTypeGroup/Edit
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        public ActionResult Edit()
        {
			// Retrieve ID from session
			string code = sessionManager.ApplicationOrganisationTypeGroupCode;
			
            ApplicationOrganisationTypeGroupVM model = new ApplicationOrganisationTypeGroupVM();
			
            // Not from staff or error
            if (String.IsNullOrEmpty(code))
            {
                //If session has lists then use them
                RepopulateListsFromCacheSession(model);

                //Assume we are in create mode as no code passed
                model.ApplicationOrganisationTypeGroupItem = new ApplicationOrganisationTypeGroupModel(){IsActive = true};
            }
            //if we have been passed a code then assume we are in edit situation and we need to retrieve from the database.
            else
            {
				// Create service instance
				IUcbService sc = UcbService;
				
                try
                {
					// Call service to get ApplicationOrganisationTypeGroup item and any associated lookups    
                    ApplicationOrganisationTypeGroupVMDC returnedObject = sc.GetApplicationOrganisationTypeGroup(CurrentUser, CurrentUser, appID, "", code);

					// Close service communication
					((ICommunicationObject)sc).Close();

                    //Get view model from service
                    model = ConvertApplicationOrganisationTypeGroupDC(returnedObject);

                    ResolveFieldCodesToFieldNamesUsingLists(model);

                    //Store the service version
                    sessionManager.ApplicationOrganisationTypeGroupServiceVersion = model.ApplicationOrganisationTypeGroupItem;
                }
				catch (Exception e)
				{
					// Handle the exception
					string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
					model.Message = message;
					
					return View(model);
				}
            }

            //Adds current retrieved ApplicationOrganisationTypeGroup to session
            sessionManager.CurrentApplicationOrganisationTypeGroup = model.ApplicationOrganisationTypeGroupItem;
            SetAccessContext(model);
            
            return View(model);
        }    
		
		#endregion
		
		#region Create/Update

        // POST: /ApplicationOrganisationTypeGroup/Edit with Create button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult CreateApplicationOrganisationTypeGroup(FormCollection collection)
        {
            return UpdateApplicationOrganisationTypeGroup();
        }

        // POST: /ApplicationOrganisationTypeGroup/Edit with Save button submitting
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult SaveApplicationOrganisationTypeGroup(FormCollection collection)
        {
            return UpdateApplicationOrganisationTypeGroup();
        }

        //This method is shared between create and save
        private ActionResult UpdateApplicationOrganisationTypeGroup()
        {
			// Get the updated model
            var model = GetUpdatedModel();

			// Test to see if there are any errors
            var errors = ModelState
                    .Where(x => x.Value.Errors.Count > 0)
                    .Select(x => new { x.Key, x.Value.Errors[0].ErrorMessage })
                    .ToArray();
            
			//Set flags false
            SetFlagsFalse(model);
			
			// Test to see if the model has validated correctly
            if (ModelState.IsValid)
            {
				// Create service instance
				IUcbService sc = UcbService;
				
                //Attempt update
                try
                {
                    // Map model to data contract
                    ApplicationOrganisationTypeGroupDC ApplicationOrganisationTypeGroupItem = Mapper.Map<ApplicationOrganisationTypeGroupDC>(model.ApplicationOrganisationTypeGroupItem);

					ApplicationOrganisationTypeGroupVMDC returnedObject = null;

                    if (null == model.ApplicationOrganisationTypeGroupItem.Code || model.ApplicationOrganisationTypeGroupItem.Code == Guid.Empty)
                    {
						// Call service to create new ApplicationOrganisationTypeGroup item
                        returnedObject = sc.CreateApplicationOrganisationTypeGroup(CurrentUser, CurrentUser, appID, "", ApplicationOrganisationTypeGroupItem);
                    }
                    else
                    {
						// Call service to update ApplicationOrganisationTypeGroup item
                        returnedObject = sc.UpdateApplicationOrganisationTypeGroup(CurrentUser, CurrentUser, appID, "", ApplicationOrganisationTypeGroupItem);
                    }

					// Close service communication
					((ICommunicationObject)sc).Close();
					
					// Retrieve item returned by service
                    var createdApplicationOrganisationTypeGroup = returnedObject.ApplicationOrganisationTypeGroupItem;

					// Map data contract to model
                    model.ApplicationOrganisationTypeGroupItem = Mapper.Map<ApplicationOrganisationTypeGroupModel>(createdApplicationOrganisationTypeGroup);
					
                    //After creation some of the fields are display only so we need the resolved look up nmames
                    ResolveFieldCodesToFieldNamesUsingLists(model);

					// Set access context to Edit mode
                    model.AccessContext = ApplicationOrganisationTypeGroupAccessContext.Edit;

					// Save version of item returned by service into session
                    sessionManager.ApplicationOrganisationTypeGroupServiceVersion = model.ApplicationOrganisationTypeGroupItem;
                    sessionManager.CurrentApplicationOrganisationTypeGroup = model.ApplicationOrganisationTypeGroupItem;
                    
                    // Remove the state from the model as these are being populated by the controller and the HTML helpers are being populated with
					// the POSTED values and not the changed ones.
                    ModelState.Clear();
                    model.Message = Resources.MESSAGE_UPDATE_SUCCEEDED;
                }
				catch (Exception e)
				{
					// Handle the exception
					string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
					model.Message = message;
					
					return View(model);
				}
            }

            return View(model);
        }

        #endregion
		
		#region Exit

        // POST: /ApplicationOrganisationTypeGroup/Edit with Exit button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult ExitApplicationOrganisationTypeGroup(FormCollection collection)
        {
            var model = GetUpdatedModel();
            if (model.IsExitConfirmed == "True" || !model.IsViewDirty)
            {
                //Set flags false
                SetFlagsFalse(model);

                model.IsExitConfirmed = "False";
                
                //remove the current values from session
                sessionManager.CurrentApplicationOrganisationTypeGroup = null;
                sessionManager.ApplicationOrganisationTypeGroupServiceVersion = null;

                return RedirectToAction("Search", "ApplicationOrganisationTypeGroup");
                
            }
            else
            {
                //Set flags false
                SetFlagsFalse(model);
                model.Message = Resources.MESSAGE_EXITCONFIRMATION;
                model.IsExitConfirmed = "True";
            }
            
            return View(model);
        }
		
		#endregion
		
		#region Delete

        // POST: /ApplicationOrganisationTypeGroup/Edit with Delete button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult DeleteApplicationOrganisationTypeGroup(FormCollection collection)
        {
            var model = GetUpdatedModel();
            if (model.IsDeleteConfirmed == "True")
            {
                //Set flags false
                SetFlagsFalse(model);
            
                model.IsDeleteConfirmed = "False";
				
				// Create service instance
				IUcbService sc = UcbService;
				
                try
                {
					// Call service to delete the item
                    sc.DeleteApplicationOrganisationTypeGroup(CurrentUser, CurrentUser, appID, "", model.ApplicationOrganisationTypeGroupItem.Code.ToString(), model.ApplicationOrganisationTypeGroupItem.RowIdentifier.ToString());

					// Close service communication
					((ICommunicationObject)sc).Close();
					
                    // Remove the current values from session
                    sessionManager.CurrentApplicationOrganisationTypeGroup = null;
                    sessionManager.ApplicationOrganisationTypeGroupServiceVersion = null;

                    // Remove the state from the model as these are being populated by the controller and the HTML helpers are being populated with
					// the POSTED values and not the changed ones.
                    ModelState.Clear();
					
					// Create new item but keep any lists
					model.ApplicationOrganisationTypeGroupItem = new ApplicationOrganisationTypeGroupModel(){IsActive = true};
					
					// Set message to return to user
                    model.Message = Resources.MESSAGE_DELETE_SUCCEEDED;
					
					// Set access context to Edit mode
                    model.AccessContext = ApplicationOrganisationTypeGroupAccessContext.Create;
					
					// Redirect to the search screen
                    return View(model);
                }
				catch (Exception e)
                {
					// Handle the exception
					string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
					model.Message = message;
					
					return View(model);
                }
            }
            else
            {
                //Set flags false
                SetFlagsFalse(model);
                model.Message = Resources.MESSAGE_DELETECONFIRMATION;
                model.IsDeleteConfirmed = "True";
            }
            
            return View(model);
        }
		
		#endregion
		
		#region New

        // POST: /ApplicationOrganisationTypeGroup/Edit with New button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamSearch(Prefix = "Search::")]
        [HttpPost]
        public ActionResult NewApplicationOrganisationTypeGroup(FormCollection collection)
        {
            var model = GetUpdatedModel();
            //Set flags false
            SetFlagsFalse(model);
            
            //Clear Down Session
			sessionManager.ApplicationOrganisationTypeGroupCode = null;
            sessionManager.CurrentApplicationOrganisationTypeGroup = null;
            sessionManager.ApplicationOrganisationTypeGroupServiceVersion = null;
            
            //Go to the Edit Screen
            return RedirectToAction("Edit", "ApplicationOrganisationTypeGroup");
        }
		
		#endregion

		#region Search

        // GET: /ApplicationOrganisationTypeGroup/Search
        //This is called when first entering search ApplicationOrganisationTypeGroup screen or when paging
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        public ActionResult Search(int page = 1)
        {   
			// Create service instance
			IUcbService sc = UcbService;
			
			// Create model
			ApplicationOrganisationTypeGroupSearchVM model = new ApplicationOrganisationTypeGroupSearchVM();
			
			try
			{
				ApplicationOrganisationTypeGroupSearchVMDC response = sc.SearchApplicationOrganisationTypeGroup(CurrentUser, CurrentUser, appID, "", null, page, PageSize, true);

				// Close service communication
				((ICommunicationObject)sc).Close();

				//Map response back to view model
				model.MatchList = Mapper.Map<IEnumerable<ApplicationOrganisationTypeGroupSearchMatchDC>, List<ApplicationOrganisationTypeGroupSearchMatchModel>>(response.MatchList);

				// Set paging values
				model.TotalRows = response.RecordCount;
				model.PageSize = sessionManager.PageSize;
				model.PageNumber = page;
				
				// Store the page number we were on
				sessionManager.ApplicationOrganisationTypeGroupPageNumber = model.PageNumber;
	        
				return View(model);
			}
			catch (Exception e)
			{
				// Handle the exception
				string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
				model.Message = message;
				
				return View(model);
			}

        }

        // POST: /ApplicationOrganisationTypeGroup/Search
        //This is called when clicking search button
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
		[HttpParamSearch(Prefix = "Search::")]
        [HttpPost]
        public ActionResult SearchPost(ApplicationOrganisationTypeGroupSearchVM model, int page = 1)
        {
		
			// Iterate through form keys
            foreach (string Key in Request.Form.Keys)
            {
				// Test if Select button was clicked...
                if (Key.StartsWith("Search::SearchPost_"))
                {
					// Retrieve ID for entity which was selected
                    Guid Value = Guid.Parse(Key.Substring(19));
									
					// Store ID for Edit screen
					sessionManager.ApplicationOrganisationTypeGroupCode = Value.ToString();
					
					// Call out to Edit screen
                    return RedirectToAction("Edit", "ApplicationOrganisationTypeGroup", new { code = Value });

                }
            }

            // Return to the Screen
            return View(model);
        }

        #endregion
		
		#region Private methods
		
        private void SetFlagsFalse(ApplicationOrganisationTypeGroupVM model)
        {
            model.IsDeleteConfirmed = "False";
            model.IsExitConfirmed = "False";
            model.IsNewConfirmed = "False";

            //Stop the binder resetting the posted values
            ModelState.Remove("IsDeleteConfirmed");
            ModelState.Remove("IsExitConfirmed");
            ModelState.Remove("IsNewConfirmed");
        }

        private void ResolveFieldCodesToFieldNamesUsingLists(ApplicationOrganisationTypeGroupVM model)
        {
			//TODO:
        }

        /// <summary>
        /// Private method to merge in the model 
        /// </summary>
        /// <returns></returns>
        private ApplicationOrganisationTypeGroupVM GetUpdatedModel()
        {
            ApplicationOrganisationTypeGroupVM model = new ApplicationOrganisationTypeGroupVM();
            RepopulateListsFromCacheSession(model);
            model.Message = "";

            if (sessionManager.CurrentApplicationOrganisationTypeGroup != null)
            {
                model.ApplicationOrganisationTypeGroupItem = sessionManager.CurrentApplicationOrganisationTypeGroup;
            }

            //***************************************NEED WHITE LIST ---- BLACK LIST ------ TO PREVENT OVERPOSTING **************************
            bool result = TryUpdateModel(model);//This also validates and sets ModelState
            //*******************************************************************************************************************************
            if (sessionManager.CurrentApplicationOrganisationTypeGroup != null)
            {
                //*****************************************PREVENT OVER POSTING ATTACKS******************************************************
                //Get the values for read only fields from session
                MergeNewValuesWithOriginal(model.ApplicationOrganisationTypeGroupItem);
                //***************************************************************************************************************************
            }

            SetAccessContext(model);

            return model;
        }
		
        private ApplicationOrganisationTypeGroupVM ConvertApplicationOrganisationTypeGroupDC(ApplicationOrganisationTypeGroupVMDC returnedObject)
        {
            ApplicationOrganisationTypeGroupVM model = new ApplicationOrganisationTypeGroupVM();
            
			// Map ApplicationOrganisationTypeGroup Item
			model.ApplicationOrganisationTypeGroupItem = Mapper.Map<ApplicationOrganisationTypeGroupDC, ApplicationOrganisationTypeGroupModel>(returnedObject.ApplicationOrganisationTypeGroupItem);
            
			// Map lookup data lists
			model.ApplicationList = Mapper.Map<IEnumerable<ApplicationDC>, List<ApplicationModel>>(returnedObject.ApplicationList);
			model.OrganisationTypeGroupList = Mapper.Map<IEnumerable<OrganisationTypeGroupDC>, List<OrganisationTypeGroupModel>>(returnedObject.OrganisationTypeGroupList);
			model.RootOrganisationForApplicationList = Mapper.Map<IEnumerable<OrganisationDC>, List<OrganisationModel>>(returnedObject.RootOrganisationForApplicationList);
        
            return model;
        }
		
		private void RepopulateListsFromCacheSession(ApplicationOrganisationTypeGroupVM model)
        {
			// Populate cached lists if they are empty. Will invoke service call
            ApplicationOrganisationTypeGroupLookupListsCacheObject CachedLists = cacheManager.ApplicationOrganisationTypeGroupListCache;

			// Retrieve any cached lists to model
			model.ApplicationList = CachedLists.ApplicationList;
			model.OrganisationTypeGroupList = CachedLists.OrganisationTypeGroupList;
			model.RootOrganisationForApplicationList = CachedLists.RootOrganisationForApplicationList;
   
        }

        private void MergeNewValuesWithOriginal(ApplicationOrganisationTypeGroupModel modelFromView)
        {
            //***************************The values that are display only will not be posted back so need to get them from session**************************

            ApplicationOrganisationTypeGroupModel OriginalValuesFromSession = sessionManager.CurrentApplicationOrganisationTypeGroup;

        }
		
		private void SetAccessContext(ApplicationOrganisationTypeGroupVM model)
        {
            //Decide on access context
            if (null == model.ApplicationOrganisationTypeGroupItem || model.ApplicationOrganisationTypeGroupItem.Code == Guid.Empty)
            {
				// Create context
                model.AccessContext = ApplicationOrganisationTypeGroupAccessContext.Create;
            }
            else
            {
				// Edit context
                model.AccessContext = ApplicationOrganisationTypeGroupAccessContext.Edit;
            }
        }
		
		private void DetermineIsDirty(ApplicationOrganisationTypeGroupVM model)
        {
            //Compare the ApplicationOrganisationTypeGroup to the original session
            if (model.ApplicationOrganisationTypeGroupItem.PublicInstancePropertiesEqual(sessionManager.ApplicationOrganisationTypeGroupServiceVersion, "RowIdentifier"))
            {
                model.IsViewDirty = false;
            }
            else
            {
                model.IsViewDirty = true;
            }

        }
        #endregion
       
    }
}
