//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Collections.Specialized;
using System.Linq;
using System.Web.Mvc;
using System.ComponentModel.DataAnnotations;
using System.ServiceModel;
using Dwp.Adep.Ucb.ResourceLibrary;
using AutoMapper;
using UcbWeb.Helpers;
using UcbWeb.ViewModels;
using UcbWeb.Models;
using UcbWeb.UcbService;


namespace UcbWeb.Controllers
{
    public partial class IntroductoryInformationController : BaseController
    {

		private IUcbService UcbService;

		// Dependency Injection enabled constructors
        public IntroductoryInformationController()
            : this(new UcbServiceClient(),new SessionManager(), new CacheManager())
        {
        }

        public IntroductoryInformationController(IUcbService UcbService, ISessionManager sessionManager, ICacheManager cacheManager)
			:base(sessionManager,cacheManager)
        {
            this.UcbService = UcbService;
        }

		#region Edit
		
        // GET: /IntroductoryInformation/Edit
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        public ActionResult Edit()
        {
			// Retrieve ID from session
			string code = sessionManager.IntroductoryInformationCode;
			
            IntroductoryInformationVM model = new IntroductoryInformationVM();
			
            // Not from staff or error
            if (String.IsNullOrEmpty(code))
            {
                //If session has lists then use them
                RepopulateListsFromCacheSession(model);

                //Assume we are in create mode as no code passed
                model.IntroductoryInformationItem = new IntroductoryInformationModel();
            }
            //if we have been passed a code then assume we are in edit situation and we need to retrieve from the database.
            else
            {
				// Create service instance
				IUcbService sc = UcbService;
				
                try
                {
					// Call service to get IntroductoryInformation item and any associated lookups    
                    IntroductoryInformationVMDC returnedObject = sc.GetIntroductoryInformation(CurrentUser, CurrentUser, appID, "", code);

					// Close service communication
					((ICommunicationObject)sc).Close();

                    //Get view model from service
                    model = ConvertIntroductoryInformationDC(returnedObject);

                    ResolveFieldCodesToFieldNamesUsingLists(model);

                    //Store the service version
                    sessionManager.IntroductoryInformationServiceVersion = model.IntroductoryInformationItem;
                }
				catch (Exception e)
				{
					// Handle the exception
					string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
					model.Message = message;
					
					return View(model);
				}
            }

            //Adds current retrieved IntroductoryInformation to session
            sessionManager.CurrentIntroductoryInformation = model.IntroductoryInformationItem;
            SetAccessContext(model);
            
            return View(model);
        }    
		
		#endregion
		
		#region Create/Update

        // POST: /IntroductoryInformation/Edit with Create button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult CreateIntroductoryInformation(FormCollection collection)
        {
            return UpdateIntroductoryInformation();
        }

        // POST: /IntroductoryInformation/Edit with Save button submitting
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult SaveIntroductoryInformation(FormCollection collection)
        {
            return UpdateIntroductoryInformation();
        }

        //This method is shared between create and save
        private ActionResult UpdateIntroductoryInformation()
        {
			// Get the updated model
            var model = GetUpdatedModel();

			// Test to see if there are any errors
            var errors = ModelState
                    .Where(x => x.Value.Errors.Count > 0)
                    .Select(x => new { x.Key, x.Value.Errors[0].ErrorMessage })
                    .ToArray();
            
			//Set flags false
            SetFlagsFalse(model);
			
			// Test to see if the model has validated correctly
            if (ModelState.IsValid)
            {
				// Create service instance
				IUcbService sc = UcbService;
				
                //Attempt update
                try
                {
                    // Map model to data contract
                    IntroductoryInformationDC IntroductoryInformationItem = Mapper.Map<IntroductoryInformationDC>(model.IntroductoryInformationItem);

					IntroductoryInformationVMDC returnedObject = null;

                    if (null == model.IntroductoryInformationItem.Code || model.IntroductoryInformationItem.Code == Guid.Empty)
                    {
						// Call service to create new IntroductoryInformation item
                        returnedObject = sc.CreateIntroductoryInformation(CurrentUser, CurrentUser, appID, "", IntroductoryInformationItem);
                    }
                    else
                    {
						// Call service to update IntroductoryInformation item
                        returnedObject = sc.UpdateIntroductoryInformation(CurrentUser, CurrentUser, appID, "", IntroductoryInformationItem);
                    }

					// Close service communication
					((ICommunicationObject)sc).Close();
					
					// Retrieve item returned by service
                    var createdIntroductoryInformation = returnedObject.IntroductoryInformationItem;

					// Map data contract to model
                    model.IntroductoryInformationItem = Mapper.Map<IntroductoryInformationModel>(createdIntroductoryInformation);
					
                    //After creation some of the fields are display only so we need the resolved look up nmames
                    ResolveFieldCodesToFieldNamesUsingLists(model);

					// Set access context to Edit mode
                    model.AccessContext = IntroductoryInformationAccessContext.Edit;

					// Save version of item returned by service into session
                    sessionManager.IntroductoryInformationServiceVersion = model.IntroductoryInformationItem;
                    sessionManager.CurrentIntroductoryInformation = model.IntroductoryInformationItem;
                    
                    // Remove the state from the model as these are being populated by the controller and the HTML helpers are being populated with
					// the POSTED values and not the changed ones.
                    ModelState.Clear();
                    model.Message = Resources.MESSAGE_UPDATE_SUCCEEDED;
                }
				catch (Exception e)
				{
					// Handle the exception
					string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
					model.Message = message;
					
					return View(model);
				}
            }

            return View(model);
        }

        #endregion
		
		#region Exit

        // POST: /IntroductoryInformation/Edit with Exit button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult ExitIntroductoryInformation(FormCollection collection)
        {
            var model = GetUpdatedModel();
            if (model.IsExitConfirmed == "True" || !model.IsViewDirty)
            {
                //Set flags false
                SetFlagsFalse(model);

                model.IsExitConfirmed = "False";
                
                //remove the current values from session
                sessionManager.CurrentIntroductoryInformation = null;
                sessionManager.IntroductoryInformationServiceVersion = null;

                return RedirectToAction("Search", "IntroductoryInformation");
                
            }
            else
            {
                //Set flags false
                SetFlagsFalse(model);
                model.Message = Resources.MESSAGE_EXITCONFIRMATION;
                model.IsExitConfirmed = "True";
            }
            
            return View(model);
        }
		
		#endregion
		
		#region Delete

        // POST: /IntroductoryInformation/Edit with Delete button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult DeleteIntroductoryInformation(FormCollection collection)
        {
            var model = GetUpdatedModel();
            if (model.IsDeleteConfirmed == "True")
            {
                //Set flags false
                SetFlagsFalse(model);
            
                model.IsDeleteConfirmed = "False";
				
				// Create service instance
				IUcbService sc = UcbService;
				
                try
                {
					// Call service to delete the item
                    sc.DeleteIntroductoryInformation(CurrentUser, CurrentUser, appID, "", model.IntroductoryInformationItem.Code.ToString(), model.IntroductoryInformationItem.RowIdentifier.ToString());

					// Close service communication
					((ICommunicationObject)sc).Close();
					
                    // Remove the current values from session
                    sessionManager.CurrentIntroductoryInformation = null;
                    sessionManager.IntroductoryInformationServiceVersion = null;

                    // Remove the state from the model as these are being populated by the controller and the HTML helpers are being populated with
					// the POSTED values and not the changed ones.
                    ModelState.Clear();
					
					// Create new item but keep any lists
					model.IntroductoryInformationItem = new IntroductoryInformationModel();
					
					// Set message to return to user
                    model.Message = Resources.MESSAGE_DELETE_SUCCEEDED;
					
					// Set access context to Edit mode
                    model.AccessContext = IntroductoryInformationAccessContext.Create;
					
					// Redirect to the search screen
                    return View(model);
                }
				catch (Exception e)
                {
					// Handle the exception
					string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
					model.Message = message;
					
					return View(model);
                }
            }
            else
            {
                //Set flags false
                SetFlagsFalse(model);
                model.Message = Resources.MESSAGE_DELETECONFIRMATION;
                model.IsDeleteConfirmed = "True";
            }
            
            return View(model);
        }
		
		#endregion
		
		#region New

        // POST: /IntroductoryInformation/Edit with New button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamSearch(Prefix = "Search::")]
        [HttpPost]
        public ActionResult NewIntroductoryInformation(FormCollection collection)
        {
            var model = GetUpdatedModel();
            //Set flags false
            SetFlagsFalse(model);
            
            //Clear Down Session
			sessionManager.IntroductoryInformationCode = null;
            sessionManager.CurrentIntroductoryInformation = null;
            sessionManager.IntroductoryInformationServiceVersion = null;
            
            //Go to the Edit Screen
            return RedirectToAction("Edit", "IntroductoryInformation");
        }
		
		#endregion

		#region Search

        // GET: /IntroductoryInformation/Search
        //This is called when first entering search IntroductoryInformation screen or when paging
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        public ActionResult Search(int page = 1)
        {   
			// Create service instance
			IUcbService sc = UcbService;
			
			// Create model
			IntroductoryInformationSearchVM model = new IntroductoryInformationSearchVM();
			
			try
			{
				IntroductoryInformationSearchVMDC response = sc.SearchIntroductoryInformation(CurrentUser, CurrentUser, appID, "", null, page, PageSize);

				// Close service communication
				((ICommunicationObject)sc).Close();

				//Map response back to view model
				model.MatchList = Mapper.Map<IEnumerable<IntroductoryInformationSearchMatchDC>, List<IntroductoryInformationSearchMatchModel>>(response.MatchList);

				// Set paging values
				model.TotalRows = response.RecordCount;
				model.PageSize = sessionManager.PageSize;
				model.PageNumber = page;
				
				// Store the page number we were on
				sessionManager.IntroductoryInformationPageNumber = model.PageNumber;
	        
				return View(model);
			}
			catch (Exception e)
			{
				// Handle the exception
				string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
				model.Message = message;
				
				return View(model);
			}

        }

        // POST: /IntroductoryInformation/Search
        //This is called when clicking search button
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
		[HttpParamSearch(Prefix = "Search::")]
        [HttpPost]
        public ActionResult SearchPost(IntroductoryInformationSearchVM model, int page = 1)
        {
		
			// Iterate through form keys
            foreach (string Key in Request.Form.Keys)
            {
				// Test if Select button was clicked...
                if (Key.StartsWith("Search::SearchPost_"))
                {
					// Retrieve ID for entity which was selected
                    Guid Value = Guid.Parse(Key.Substring(19));
									
					// Store ID for Edit screen
					sessionManager.IntroductoryInformationCode = Value.ToString();
					
					// Call out to Edit screen
                    return RedirectToAction("Edit", "IntroductoryInformation", new { code = Value });

                }
            }

            // Return to the Screen
            return View(model);
        }

        #endregion
		
		#region Private methods
		
        private void SetFlagsFalse(IntroductoryInformationVM model)
        {
            model.IsDeleteConfirmed = "False";
            model.IsExitConfirmed = "False";
            model.IsNewConfirmed = "False";

            //Stop the binder resetting the posted values
            ModelState.Remove("IsDeleteConfirmed");
            ModelState.Remove("IsExitConfirmed");
            ModelState.Remove("IsNewConfirmed");
        }

        private void ResolveFieldCodesToFieldNamesUsingLists(IntroductoryInformationVM model)
        {
			//TODO:
        }

        /// <summary>
        /// Private method to merge in the model 
        /// </summary>
        /// <returns></returns>
        private IntroductoryInformationVM GetUpdatedModel()
        {
            IntroductoryInformationVM model = new IntroductoryInformationVM();
            RepopulateListsFromCacheSession(model);
            model.Message = "";

            if (sessionManager.CurrentIntroductoryInformation != null)
            {
                model.IntroductoryInformationItem = sessionManager.CurrentIntroductoryInformation;
            }

            //***************************************NEED WHITE LIST ---- BLACK LIST ------ TO PREVENT OVERPOSTING **************************
            bool result = TryUpdateModel(model);//This also validates and sets ModelState
            //*******************************************************************************************************************************
            if (sessionManager.CurrentIntroductoryInformation != null)
            {
                //*****************************************PREVENT OVER POSTING ATTACKS******************************************************
                //Get the values for read only fields from session
                MergeNewValuesWithOriginal(model.IntroductoryInformationItem);
                //***************************************************************************************************************************
            }

            SetAccessContext(model);

            return model;
        }
		
        private IntroductoryInformationVM ConvertIntroductoryInformationDC(IntroductoryInformationVMDC returnedObject)
        {
            IntroductoryInformationVM model = new IntroductoryInformationVM();
            
			// Map IntroductoryInformation Item
			model.IntroductoryInformationItem = Mapper.Map<IntroductoryInformationDC, IntroductoryInformationModel>(returnedObject.IntroductoryInformationItem);
            
			// Map lookup data lists
        
            return model;
        }
		
		private void RepopulateListsFromCacheSession(IntroductoryInformationVM model)
        {
			// Populate cached lists if they are empty. Will invoke service call
            IntroductoryInformationLookupListsCacheObject CachedLists = cacheManager.IntroductoryInformationListCache;

			// Retrieve any cached lists to model
   
        }

        private void MergeNewValuesWithOriginal(IntroductoryInformationModel modelFromView)
        {
            //***************************The values that are display only will not be posted back so need to get them from session**************************

            IntroductoryInformationModel OriginalValuesFromSession = sessionManager.CurrentIntroductoryInformation;

        }
		
		private void SetAccessContext(IntroductoryInformationVM model)
        {
            //Decide on access context
            if (null == model.IntroductoryInformationItem || model.IntroductoryInformationItem.Code == Guid.Empty)
            {
				// Create context
                model.AccessContext = IntroductoryInformationAccessContext.Create;
            }
            else
            {
				// Edit context
                model.AccessContext = IntroductoryInformationAccessContext.Edit;
            }
        }
		
		private void DetermineIsDirty(IntroductoryInformationVM model)
        {
            //Compare the IntroductoryInformation to the original session
            if (model.IntroductoryInformationItem.PublicInstancePropertiesEqual(sessionManager.IntroductoryInformationServiceVersion, "RowIdentifier"))
            {
                model.IsViewDirty = false;
            }
            else
            {
                model.IsViewDirty = true;
            }

        }
        #endregion
       
    }
}
