//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Collections.Specialized;
using System.Linq;
using System.Web.Mvc;
using System.ComponentModel.DataAnnotations;
using System.ServiceModel;
using Dwp.Adep.Ucb.ResourceLibrary;
using AutoMapper;
using UcbWeb.Helpers;
using UcbWeb.ViewModels;
using UcbWeb.Models;
using UcbWeb.UcbService;


namespace UcbWeb.Controllers
{
    public partial class EventLeadingToIncidentController : BaseController
    {

		private IUcbService UcbService;

		// Dependency Injection enabled constructors
        public EventLeadingToIncidentController()
            : this(new UcbServiceClient(),new SessionManager(), new CacheManager())
        {
        }

        public EventLeadingToIncidentController(IUcbService UcbService, ISessionManager sessionManager, ICacheManager cacheManager)
			:base(sessionManager,cacheManager)
        {
            this.UcbService = UcbService;
        }

		#region Edit
		
        // GET: /EventLeadingToIncident/Edit
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        public ActionResult Edit()
        {
			// Retrieve ID from session
			string code = sessionManager.EventLeadingToIncidentCode;
			
            EventLeadingToIncidentVM model = new EventLeadingToIncidentVM();
			
            // Not from staff or error
            if (String.IsNullOrEmpty(code))
            {
                //If session has lists then use them
                RepopulateListsFromCacheSession(model);

                //Assume we are in create mode as no code passed
                model.EventLeadingToIncidentItem = new EventLeadingToIncidentModel(){IsActive = true};
            }
            //if we have been passed a code then assume we are in edit situation and we need to retrieve from the database.
            else
            {
				// Create service instance
				IUcbService sc = UcbService;
				
                try
                {
					// Call service to get EventLeadingToIncident item and any associated lookups    
                    EventLeadingToIncidentVMDC returnedObject = sc.GetEventLeadingToIncident(CurrentUser, CurrentUser, appID, "", code);

					// Close service communication
					((ICommunicationObject)sc).Close();

                    //Get view model from service
                    model = ConvertEventLeadingToIncidentDC(returnedObject);

                    ResolveFieldCodesToFieldNamesUsingLists(model);

                    //Store the service version
                    sessionManager.EventLeadingToIncidentServiceVersion = model.EventLeadingToIncidentItem;
                }
				catch (Exception e)
				{
					// Handle the exception
					string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
					model.Message = message;
					
					return View(model);
				}
            }

            //Adds current retrieved EventLeadingToIncident to session
            sessionManager.CurrentEventLeadingToIncident = model.EventLeadingToIncidentItem;
            SetAccessContext(model);
            
            return View(model);
        }    
		
		#endregion
		
		#region Create/Update

        // POST: /EventLeadingToIncident/Edit with Create button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult CreateEventLeadingToIncident(FormCollection collection)
        {
            return UpdateEventLeadingToIncident();
        }

        // POST: /EventLeadingToIncident/Edit with Save button submitting
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult SaveEventLeadingToIncident(FormCollection collection)
        {
            return UpdateEventLeadingToIncident();
        }

        //This method is shared between create and save
        private ActionResult UpdateEventLeadingToIncident()
        {
			// Get the updated model
            var model = GetUpdatedModel();

			// Test to see if there are any errors
            var errors = ModelState
                    .Where(x => x.Value.Errors.Count > 0)
                    .Select(x => new { x.Key, x.Value.Errors[0].ErrorMessage })
                    .ToArray();
            
			//Set flags false
            SetFlagsFalse(model);
			
			// Test to see if the model has validated correctly
            if (ModelState.IsValid)
            {
				// Create service instance
				IUcbService sc = UcbService;
				
                //Attempt update
                try
                {
                    // Map model to data contract
                    EventLeadingToIncidentDC EventLeadingToIncidentItem = Mapper.Map<EventLeadingToIncidentDC>(model.EventLeadingToIncidentItem);

					EventLeadingToIncidentVMDC returnedObject = null;

                    if (null == model.EventLeadingToIncidentItem.Code || model.EventLeadingToIncidentItem.Code == Guid.Empty)
                    {
						// Call service to create new EventLeadingToIncident item
                        returnedObject = sc.CreateEventLeadingToIncident(CurrentUser, CurrentUser, appID, "", EventLeadingToIncidentItem);
                    }
                    else
                    {
						// Call service to update EventLeadingToIncident item
                        returnedObject = sc.UpdateEventLeadingToIncident(CurrentUser, CurrentUser, appID, "", EventLeadingToIncidentItem);
                    }

					// Close service communication
					((ICommunicationObject)sc).Close();
					
					// Retrieve item returned by service
                    var createdEventLeadingToIncident = returnedObject.EventLeadingToIncidentItem;

					// Map data contract to model
                    model.EventLeadingToIncidentItem = Mapper.Map<EventLeadingToIncidentModel>(createdEventLeadingToIncident);
					
                    //After creation some of the fields are display only so we need the resolved look up nmames
                    ResolveFieldCodesToFieldNamesUsingLists(model);

					// Set access context to Edit mode
                    model.AccessContext = EventLeadingToIncidentAccessContext.Edit;

					// Save version of item returned by service into session
                    sessionManager.EventLeadingToIncidentServiceVersion = model.EventLeadingToIncidentItem;
                    sessionManager.CurrentEventLeadingToIncident = model.EventLeadingToIncidentItem;
                    
                    // Remove the state from the model as these are being populated by the controller and the HTML helpers are being populated with
					// the POSTED values and not the changed ones.
                    ModelState.Clear();
                    model.Message = Resources.MESSAGE_UPDATE_SUCCEEDED;
                }
				catch (Exception e)
				{
					// Handle the exception
					string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
					model.Message = message;
					
					return View(model);
				}
            }

            return View(model);
        }

        #endregion
		
		#region Exit

        // POST: /EventLeadingToIncident/Edit with Exit button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult ExitEventLeadingToIncident(FormCollection collection)
        {
            var model = GetUpdatedModel();
            if (model.IsExitConfirmed == "True" || !model.IsViewDirty)
            {
                //Set flags false
                SetFlagsFalse(model);

                model.IsExitConfirmed = "False";
                
                //remove the current values from session
                sessionManager.CurrentEventLeadingToIncident = null;
                sessionManager.EventLeadingToIncidentServiceVersion = null;

                return RedirectToAction("Search", "EventLeadingToIncident");
                
            }
            else
            {
                //Set flags false
                SetFlagsFalse(model);
                model.Message = Resources.MESSAGE_EXITCONFIRMATION;
                model.IsExitConfirmed = "True";
            }
            
            return View(model);
        }
		
		#endregion
		
		#region Delete

        // POST: /EventLeadingToIncident/Edit with Delete button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult DeleteEventLeadingToIncident(FormCollection collection)
        {
            var model = GetUpdatedModel();
            if (model.IsDeleteConfirmed == "True")
            {
                //Set flags false
                SetFlagsFalse(model);
            
                model.IsDeleteConfirmed = "False";
				
				// Create service instance
				IUcbService sc = UcbService;
				
                try
                {
					// Call service to delete the item
                    sc.DeleteEventLeadingToIncident(CurrentUser, CurrentUser, appID, "", model.EventLeadingToIncidentItem.Code.ToString(), model.EventLeadingToIncidentItem.RowIdentifier.ToString());

					// Close service communication
					((ICommunicationObject)sc).Close();
					
                    // Remove the current values from session
                    sessionManager.CurrentEventLeadingToIncident = null;
                    sessionManager.EventLeadingToIncidentServiceVersion = null;

                    // Remove the state from the model as these are being populated by the controller and the HTML helpers are being populated with
					// the POSTED values and not the changed ones.
                    ModelState.Clear();
					
					// Create new item but keep any lists
					model.EventLeadingToIncidentItem = new EventLeadingToIncidentModel(){IsActive = true};
					
					// Set message to return to user
                    model.Message = Resources.MESSAGE_DELETE_SUCCEEDED;
					
					// Set access context to Edit mode
                    model.AccessContext = EventLeadingToIncidentAccessContext.Create;
					
					// Redirect to the search screen
                    return View(model);
                }
				catch (Exception e)
                {
					// Handle the exception
					string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
					model.Message = message;
					
					return View(model);
                }
            }
            else
            {
                //Set flags false
                SetFlagsFalse(model);
                model.Message = Resources.MESSAGE_DELETECONFIRMATION;
                model.IsDeleteConfirmed = "True";
            }
            
            return View(model);
        }
		
		#endregion
		
		#region New

        // POST: /EventLeadingToIncident/Edit with New button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamSearch(Prefix = "Search::")]
        [HttpPost]
        public ActionResult NewEventLeadingToIncident(FormCollection collection)
        {
            var model = GetUpdatedModel();
            //Set flags false
            SetFlagsFalse(model);
            
            //Clear Down Session
			sessionManager.EventLeadingToIncidentCode = null;
            sessionManager.CurrentEventLeadingToIncident = null;
            sessionManager.EventLeadingToIncidentServiceVersion = null;
            
            //Go to the Edit Screen
            return RedirectToAction("Edit", "EventLeadingToIncident");
        }
		
		#endregion

		#region Search

        // GET: /EventLeadingToIncident/Search
        //This is called when first entering search EventLeadingToIncident screen or when paging
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        public ActionResult Search(int page = 1)
        {   
			// Create service instance
			IUcbService sc = UcbService;
			
			// Create model
			EventLeadingToIncidentSearchVM model = new EventLeadingToIncidentSearchVM();
			
			try
			{
				EventLeadingToIncidentSearchVMDC response = sc.SearchEventLeadingToIncident(CurrentUser, CurrentUser, appID, "", null, page, PageSize, true);

				// Close service communication
				((ICommunicationObject)sc).Close();

				//Map response back to view model
				model.MatchList = Mapper.Map<IEnumerable<EventLeadingToIncidentSearchMatchDC>, List<EventLeadingToIncidentSearchMatchModel>>(response.MatchList);

				// Set paging values
				model.TotalRows = response.RecordCount;
				model.PageSize = sessionManager.PageSize;
				model.PageNumber = page;
				
				// Store the page number we were on
				sessionManager.EventLeadingToIncidentPageNumber = model.PageNumber;
	        
				return View(model);
			}
			catch (Exception e)
			{
				// Handle the exception
				string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
				model.Message = message;
				
				return View(model);
			}

        }

        // POST: /EventLeadingToIncident/Search
        //This is called when clicking search button
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
		[HttpParamSearch(Prefix = "Search::")]
        [HttpPost]
        public ActionResult SearchPost(EventLeadingToIncidentSearchVM model, int page = 1)
        {
		
			// Iterate through form keys
            foreach (string Key in Request.Form.Keys)
            {
				// Test if Select button was clicked...
                if (Key.StartsWith("Search::SearchPost_"))
                {
					// Retrieve ID for entity which was selected
                    Guid Value = Guid.Parse(Key.Substring(19));
									
					// Store ID for Edit screen
					sessionManager.EventLeadingToIncidentCode = Value.ToString();
					
					// Call out to Edit screen
                    return RedirectToAction("Edit", "EventLeadingToIncident", new { code = Value });

                }
            }

            // Return to the Screen
            return View(model);
        }

        #endregion
		
		#region Private methods
		
        private void SetFlagsFalse(EventLeadingToIncidentVM model)
        {
            model.IsDeleteConfirmed = "False";
            model.IsExitConfirmed = "False";
            model.IsNewConfirmed = "False";

            //Stop the binder resetting the posted values
            ModelState.Remove("IsDeleteConfirmed");
            ModelState.Remove("IsExitConfirmed");
            ModelState.Remove("IsNewConfirmed");
        }

        private void ResolveFieldCodesToFieldNamesUsingLists(EventLeadingToIncidentVM model)
        {
			//TODO:
        }

        /// <summary>
        /// Private method to merge in the model 
        /// </summary>
        /// <returns></returns>
        private EventLeadingToIncidentVM GetUpdatedModel()
        {
            EventLeadingToIncidentVM model = new EventLeadingToIncidentVM();
            RepopulateListsFromCacheSession(model);
            model.Message = "";

            if (sessionManager.CurrentEventLeadingToIncident != null)
            {
                model.EventLeadingToIncidentItem = sessionManager.CurrentEventLeadingToIncident;
            }

            //***************************************NEED WHITE LIST ---- BLACK LIST ------ TO PREVENT OVERPOSTING **************************
            bool result = TryUpdateModel(model);//This also validates and sets ModelState
            //*******************************************************************************************************************************
            if (sessionManager.CurrentEventLeadingToIncident != null)
            {
                //*****************************************PREVENT OVER POSTING ATTACKS******************************************************
                //Get the values for read only fields from session
                MergeNewValuesWithOriginal(model.EventLeadingToIncidentItem);
                //***************************************************************************************************************************
            }

            SetAccessContext(model);

            return model;
        }
		
        private EventLeadingToIncidentVM ConvertEventLeadingToIncidentDC(EventLeadingToIncidentVMDC returnedObject)
        {
            EventLeadingToIncidentVM model = new EventLeadingToIncidentVM();
            
			// Map EventLeadingToIncident Item
			model.EventLeadingToIncidentItem = Mapper.Map<EventLeadingToIncidentDC, EventLeadingToIncidentModel>(returnedObject.EventLeadingToIncidentItem);
            
			// Map lookup data lists
        
            return model;
        }
		
		private void RepopulateListsFromCacheSession(EventLeadingToIncidentVM model)
        {
			// Populate cached lists if they are empty. Will invoke service call
            EventLeadingToIncidentLookupListsCacheObject CachedLists = cacheManager.EventLeadingToIncidentListCache;

			// Retrieve any cached lists to model
   
        }

        private void MergeNewValuesWithOriginal(EventLeadingToIncidentModel modelFromView)
        {
            //***************************The values that are display only will not be posted back so need to get them from session**************************

            EventLeadingToIncidentModel OriginalValuesFromSession = sessionManager.CurrentEventLeadingToIncident;

        }
		
		private void SetAccessContext(EventLeadingToIncidentVM model)
        {
            //Decide on access context
            if (null == model.EventLeadingToIncidentItem || model.EventLeadingToIncidentItem.Code == Guid.Empty)
            {
				// Create context
                model.AccessContext = EventLeadingToIncidentAccessContext.Create;
            }
            else
            {
				// Edit context
                model.AccessContext = EventLeadingToIncidentAccessContext.Edit;
            }
        }
		
		private void DetermineIsDirty(EventLeadingToIncidentVM model)
        {
            //Compare the EventLeadingToIncident to the original session
            if (model.EventLeadingToIncidentItem.PublicInstancePropertiesEqual(sessionManager.EventLeadingToIncidentServiceVersion, "RowIdentifier"))
            {
                model.IsViewDirty = false;
            }
            else
            {
                model.IsViewDirty = true;
            }

        }
        #endregion
       
    }
}
