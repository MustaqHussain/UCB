//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Collections.Specialized;
using System.Linq;
using System.Web.Mvc;
using System.ComponentModel.DataAnnotations;
using System.ServiceModel;
using Dwp.Adep.Ucb.ResourceLibrary;
using AutoMapper;
using UcbWeb.Helpers;
using UcbWeb.ViewModels;
using UcbWeb.Models;
using UcbWeb.UcbService;


namespace UcbWeb.Controllers
{
    public partial class IncidentInterestedPartyController : BaseController
    {

		private IUcbService UcbService;

		// Dependency Injection enabled constructors
        public IncidentInterestedPartyController()
            : this(new UcbServiceClient(),new SessionManager(), new CacheManager())
        {
        }

        public IncidentInterestedPartyController(IUcbService UcbService, ISessionManager sessionManager, ICacheManager cacheManager)
			:base(sessionManager,cacheManager)
        {
            this.UcbService = UcbService;
        }

		#region Edit
		
        // GET: /IncidentInterestedParty/Edit
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        public ActionResult Edit()
        {
			// Retrieve ID from session
			string code = sessionManager.IncidentInterestedPartyCode;
			
            IncidentInterestedPartyVM model = new IncidentInterestedPartyVM();
			
            // Not from staff or error
            if (String.IsNullOrEmpty(code))
            {
                //If session has lists then use them
                RepopulateListsFromCacheSession(model);

                //Assume we are in create mode as no code passed
                model.IncidentInterestedPartyItem = new IncidentInterestedPartyModel();
            }
            //if we have been passed a code then assume we are in edit situation and we need to retrieve from the database.
            else
            {
				// Create service instance
				IUcbService sc = UcbService;
				
                try
                {
					// Call service to get IncidentInterestedParty item and any associated lookups    
                    IncidentInterestedPartyVMDC returnedObject = sc.GetIncidentInterestedParty(CurrentUser, CurrentUser, appID, "", code);

					// Close service communication
					((ICommunicationObject)sc).Close();

                    //Get view model from service
                    model = ConvertIncidentInterestedPartyDC(returnedObject);

                    ResolveFieldCodesToFieldNamesUsingLists(model);

                    //Store the service version
                    sessionManager.IncidentInterestedPartyServiceVersion = model.IncidentInterestedPartyItem;
                }
				catch (Exception e)
				{
					// Handle the exception
					string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
					model.Message = message;
					
					return View(model);
				}
            }

            //Adds current retrieved IncidentInterestedParty to session
            sessionManager.CurrentIncidentInterestedParty = model.IncidentInterestedPartyItem;
            SetAccessContext(model);
            
            return View(model);
        }    
		
		#endregion
		
		#region Create/Update

        // POST: /IncidentInterestedParty/Edit with Create button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult CreateIncidentInterestedParty(FormCollection collection)
        {
            return UpdateIncidentInterestedParty();
        }

        // POST: /IncidentInterestedParty/Edit with Save button submitting
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult SaveIncidentInterestedParty(FormCollection collection)
        {
            return UpdateIncidentInterestedParty();
        }

        //This method is shared between create and save
        private ActionResult UpdateIncidentInterestedParty()
        {
			// Get the updated model
            var model = GetUpdatedModel();

			// Test to see if there are any errors
            var errors = ModelState
                    .Where(x => x.Value.Errors.Count > 0)
                    .Select(x => new { x.Key, x.Value.Errors[0].ErrorMessage })
                    .ToArray();
            
			//Set flags false
            SetFlagsFalse(model);
			
			// Test to see if the model has validated correctly
            if (ModelState.IsValid)
            {
				// Create service instance
				IUcbService sc = UcbService;
				
                //Attempt update
                try
                {
                    // Map model to data contract
                    IncidentInterestedPartyDC IncidentInterestedPartyItem = Mapper.Map<IncidentInterestedPartyDC>(model.IncidentInterestedPartyItem);

					IncidentInterestedPartyVMDC returnedObject = null;

                    if (null == model.IncidentInterestedPartyItem.Code || model.IncidentInterestedPartyItem.Code == Guid.Empty)
                    {
						// Call service to create new IncidentInterestedParty item
                        returnedObject = sc.CreateIncidentInterestedParty(CurrentUser, CurrentUser, appID, "", IncidentInterestedPartyItem);
                    }
                    else
                    {
						// Call service to update IncidentInterestedParty item
                        returnedObject = sc.UpdateIncidentInterestedParty(CurrentUser, CurrentUser, appID, "", IncidentInterestedPartyItem);
                    }

					// Close service communication
					((ICommunicationObject)sc).Close();
					
					// Retrieve item returned by service
                    var createdIncidentInterestedParty = returnedObject.IncidentInterestedPartyItem;

					// Map data contract to model
                    model.IncidentInterestedPartyItem = Mapper.Map<IncidentInterestedPartyModel>(createdIncidentInterestedParty);
					
                    //After creation some of the fields are display only so we need the resolved look up nmames
                    ResolveFieldCodesToFieldNamesUsingLists(model);

					// Set access context to Edit mode
                    model.AccessContext = IncidentInterestedPartyAccessContext.Edit;

					// Save version of item returned by service into session
                    sessionManager.IncidentInterestedPartyServiceVersion = model.IncidentInterestedPartyItem;
                    sessionManager.CurrentIncidentInterestedParty = model.IncidentInterestedPartyItem;
                    
                    // Remove the state from the model as these are being populated by the controller and the HTML helpers are being populated with
					// the POSTED values and not the changed ones.
                    ModelState.Clear();
                    model.Message = Resources.MESSAGE_UPDATE_SUCCEEDED;
                }
				catch (Exception e)
				{
					// Handle the exception
					string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
					model.Message = message;
					
					return View(model);
				}
            }

            return View(model);
        }

        #endregion
		
		#region Exit

        // POST: /IncidentInterestedParty/Edit with Exit button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult ExitIncidentInterestedParty(FormCollection collection)
        {
            var model = GetUpdatedModel();
            if (model.IsExitConfirmed == "True" || !model.IsViewDirty)
            {
                //Set flags false
                SetFlagsFalse(model);

                model.IsExitConfirmed = "False";
                
                //remove the current values from session
                sessionManager.CurrentIncidentInterestedParty = null;
                sessionManager.IncidentInterestedPartyServiceVersion = null;

                return RedirectToAction("Search", "IncidentInterestedParty");
                
            }
            else
            {
                //Set flags false
                SetFlagsFalse(model);
                model.Message = Resources.MESSAGE_EXITCONFIRMATION;
                model.IsExitConfirmed = "True";
            }
            
            return View(model);
        }
		
		#endregion
		
		#region Delete

        // POST: /IncidentInterestedParty/Edit with Delete button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamEdit(Prefix = "Edit::")]
        [HttpPost]
        public ActionResult DeleteIncidentInterestedParty(FormCollection collection)
        {
            var model = GetUpdatedModel();
            if (model.IsDeleteConfirmed == "True")
            {
                //Set flags false
                SetFlagsFalse(model);
            
                model.IsDeleteConfirmed = "False";
				
				// Create service instance
				IUcbService sc = UcbService;
				
                try
                {
					// Call service to delete the item
                    sc.DeleteIncidentInterestedParty(CurrentUser, CurrentUser, appID, "", model.IncidentInterestedPartyItem.Code.ToString(), model.IncidentInterestedPartyItem.RowIdentifier.ToString());

					// Close service communication
					((ICommunicationObject)sc).Close();
					
                    // Remove the current values from session
                    sessionManager.CurrentIncidentInterestedParty = null;
                    sessionManager.IncidentInterestedPartyServiceVersion = null;

                    // Remove the state from the model as these are being populated by the controller and the HTML helpers are being populated with
					// the POSTED values and not the changed ones.
                    ModelState.Clear();
					
					// Create new item but keep any lists
					model.IncidentInterestedPartyItem = new IncidentInterestedPartyModel();
					
					// Set message to return to user
                    model.Message = Resources.MESSAGE_DELETE_SUCCEEDED;
					
					// Set access context to Edit mode
                    model.AccessContext = IncidentInterestedPartyAccessContext.Create;
					
					// Redirect to the search screen
                    return View(model);
                }
				catch (Exception e)
                {
					// Handle the exception
					string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
					model.Message = message;
					
					return View(model);
                }
            }
            else
            {
                //Set flags false
                SetFlagsFalse(model);
                model.Message = Resources.MESSAGE_DELETECONFIRMATION;
                model.IsDeleteConfirmed = "True";
            }
            
            return View(model);
        }
		
		#endregion
		
		#region New

        // POST: /IncidentInterestedParty/Edit with New button submitting
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        [HttpParamSearch(Prefix = "Search::")]
        [HttpPost]
        public ActionResult NewIncidentInterestedParty(FormCollection collection)
        {
            var model = GetUpdatedModel();
            //Set flags false
            SetFlagsFalse(model);
            
            //Clear Down Session
			sessionManager.IncidentInterestedPartyCode = null;
            sessionManager.CurrentIncidentInterestedParty = null;
            sessionManager.IncidentInterestedPartyServiceVersion = null;
            
            //Go to the Edit Screen
            return RedirectToAction("Edit", "IncidentInterestedParty");
        }
		
		#endregion

		#region Search

        // GET: /IncidentInterestedParty/Search
        //This is called when first entering search IncidentInterestedParty screen or when paging
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
        public ActionResult Search(int page = 1)
        {   
			// Create service instance
			IUcbService sc = UcbService;
			
			// Create model
			IncidentInterestedPartySearchVM model = new IncidentInterestedPartySearchVM();
			
			try
			{
				IncidentInterestedPartySearchVMDC response = sc.SearchIncidentInterestedParty(CurrentUser, CurrentUser, appID, "", null, page, PageSize);

				// Close service communication
				((ICommunicationObject)sc).Close();

				//Map response back to view model
				model.MatchList = Mapper.Map<IEnumerable<IncidentInterestedPartySearchMatchDC>, List<IncidentInterestedPartySearchMatchModel>>(response.MatchList);

				// Set paging values
				model.TotalRows = response.RecordCount;
				model.PageSize = sessionManager.PageSize;
				model.PageNumber = page;
				
				// Store the page number we were on
				sessionManager.IncidentInterestedPartyPageNumber = model.PageNumber;
	        
				return View(model);
			}
			catch (Exception e)
			{
				// Handle the exception
				string message = ExceptionManager.HandleException(e, (ICommunicationObject)sc);
				model.Message = message;
				
				return View(model);
			}

        }

        // POST: /IncidentInterestedParty/Search
        //This is called when clicking search button
		[CustomAuthorize(Roles = AppRoles.ADMIN)]
		[HttpParamSearch(Prefix = "Search::")]
        [HttpPost]
        public ActionResult SearchPost(IncidentInterestedPartySearchVM model, int page = 1)
        {
		
			// Iterate through form keys
            foreach (string Key in Request.Form.Keys)
            {
				// Test if Select button was clicked...
                if (Key.StartsWith("Search::SearchPost_"))
                {
					// Retrieve ID for entity which was selected
                    Guid Value = Guid.Parse(Key.Substring(19));
									
					// Store ID for Edit screen
					sessionManager.IncidentInterestedPartyCode = Value.ToString();
					
					// Call out to Edit screen
                    return RedirectToAction("Edit", "IncidentInterestedParty", new { code = Value });

                }
            }

            // Return to the Screen
            return View(model);
        }

        #endregion
		
		#region Private methods
		
        private void SetFlagsFalse(IncidentInterestedPartyVM model)
        {
            model.IsDeleteConfirmed = "False";
            model.IsExitConfirmed = "False";
            model.IsNewConfirmed = "False";

            //Stop the binder resetting the posted values
            ModelState.Remove("IsDeleteConfirmed");
            ModelState.Remove("IsExitConfirmed");
            ModelState.Remove("IsNewConfirmed");
        }

        private void ResolveFieldCodesToFieldNamesUsingLists(IncidentInterestedPartyVM model)
        {
			//TODO:
        }

        /// <summary>
        /// Private method to merge in the model 
        /// </summary>
        /// <returns></returns>
        private IncidentInterestedPartyVM GetUpdatedModel()
        {
            IncidentInterestedPartyVM model = new IncidentInterestedPartyVM();
            RepopulateListsFromCacheSession(model);
            model.Message = "";

            if (sessionManager.CurrentIncidentInterestedParty != null)
            {
                model.IncidentInterestedPartyItem = sessionManager.CurrentIncidentInterestedParty;
            }

            //***************************************NEED WHITE LIST ---- BLACK LIST ------ TO PREVENT OVERPOSTING **************************
            bool result = TryUpdateModel(model);//This also validates and sets ModelState
            //*******************************************************************************************************************************
            if (sessionManager.CurrentIncidentInterestedParty != null)
            {
                //*****************************************PREVENT OVER POSTING ATTACKS******************************************************
                //Get the values for read only fields from session
                MergeNewValuesWithOriginal(model.IncidentInterestedPartyItem);
                //***************************************************************************************************************************
            }

            SetAccessContext(model);

            return model;
        }
		
        private IncidentInterestedPartyVM ConvertIncidentInterestedPartyDC(IncidentInterestedPartyVMDC returnedObject)
        {
            IncidentInterestedPartyVM model = new IncidentInterestedPartyVM();
            
			// Map IncidentInterestedParty Item
			model.IncidentInterestedPartyItem = Mapper.Map<IncidentInterestedPartyDC, IncidentInterestedPartyModel>(returnedObject.IncidentInterestedPartyItem);
            
			// Map lookup data lists
			model.IncidentList = Mapper.Map<IEnumerable<IncidentDC>, List<IncidentModel>>(returnedObject.IncidentList);
			model.InterestedPartyList = Mapper.Map<IEnumerable<InterestedPartyDC>, List<InterestedPartyModel>>(returnedObject.InterestedPartyList);
        
            return model;
        }
		
		private void RepopulateListsFromCacheSession(IncidentInterestedPartyVM model)
        {
			// Populate cached lists if they are empty. Will invoke service call
            IncidentInterestedPartyLookupListsCacheObject CachedLists = cacheManager.IncidentInterestedPartyListCache;

			// Retrieve any cached lists to model
			model.IncidentList = CachedLists.IncidentList;
			model.InterestedPartyList = CachedLists.InterestedPartyList;
   
        }

        private void MergeNewValuesWithOriginal(IncidentInterestedPartyModel modelFromView)
        {
            //***************************The values that are display only will not be posted back so need to get them from session**************************

            IncidentInterestedPartyModel OriginalValuesFromSession = sessionManager.CurrentIncidentInterestedParty;

        }
		
		private void SetAccessContext(IncidentInterestedPartyVM model)
        {
            //Decide on access context
            if (null == model.IncidentInterestedPartyItem || model.IncidentInterestedPartyItem.Code == Guid.Empty)
            {
				// Create context
                model.AccessContext = IncidentInterestedPartyAccessContext.Create;
            }
            else
            {
				// Edit context
                model.AccessContext = IncidentInterestedPartyAccessContext.Edit;
            }
        }
		
		private void DetermineIsDirty(IncidentInterestedPartyVM model)
        {
            //Compare the IncidentInterestedParty to the original session
            if (model.IncidentInterestedPartyItem.PublicInstancePropertiesEqual(sessionManager.IncidentInterestedPartyServiceVersion, "RowIdentifier"))
            {
                model.IsViewDirty = false;
            }
            else
            {
                model.IsViewDirty = true;
            }

        }
        #endregion
       
    }
}
