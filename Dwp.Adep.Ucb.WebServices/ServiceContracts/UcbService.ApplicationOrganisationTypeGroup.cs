//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using System;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Collections.Specialized;
using System.Linq;
using System.Runtime.Serialization;
using System.ServiceModel;
using AutoMapper;
using Dwp.Adep.Ucb.WebServices.DataContracts;
using Dwp.Adep.Ucb.WebServices.Exceptions;
using Dwp.Adep.Ucb.Mapping;
using Dwp.Adep.Ucb.DataServices;
using Dwp.Adep.Ucb.DataServices.Models;
using Dwp.Adep.Ucb.WebServices.MessageContracts.Exceptions;
using Dwp.Adep.Ucb.WebServices.ServiceContracts;

namespace Dwp.Adep.Ucb.WebServices.ServiceContracts
{
        #region generated code for ApplicationOrganisationTypeGroup
    /// <summary>
    /// Service
    /// Class containing service behaviour for ApplicationOrganisationTypeGroup
    /// </summary>
    public partial class UcbService
    {
            #region Behaviour for ApplicationOrganisationTypeGroup
    
    		#region Create
    
    		/// <summary>
            /// Create a ApplicationOrganisationTypeGroup
            /// </summary>
            /// <param name="currentUser"></param>
            /// <param name="user"></param>
            /// <param name="appID"></param>
            /// <param name="overrideID"></param>
            /// <param name="dc"></param>
            public ApplicationOrganisationTypeGroupVMDC CreateApplicationOrganisationTypeGroup(string currentUser, string user, string appID, string overrideID, ApplicationOrganisationTypeGroupDC dc)
            {
    			// Create unit of work
                IUnitOfWork uow = new UnitOfWork(currentUser);
    	
    			// Create repository
                Repository<ApplicationOrganisationTypeGroup> dataRepository = new Repository<ApplicationOrganisationTypeGroup>(uow.ObjectContext, currentUser, user, appID, overrideID);
    			
    			//Create ExceptionManager
    			IExceptionManager exceptionManager = new ExceptionManager();
    			
    			//Create MappingService
    			IMappingService mappingService = new MappingService();
    			
    			
    			// Call overload with injected objects
                return CreateApplicationOrganisationTypeGroup(currentUser, user, appID, overrideID, dc, dataRepository, uow,exceptionManager, mappingService);
            }
    
    		/// <summary>
            ///  Create a ApplicationOrganisationTypeGroup
            /// </summary>
            /// <param name="currentUser"></param>
            /// <param name="user"></param>
            /// <param name="appID"></param>
            /// <param name="overrideID"></param>
            /// <param name="dc"></param>
            /// <param name="dataRepository"></param>
            /// <param name="uow"></param>
            public ApplicationOrganisationTypeGroupVMDC CreateApplicationOrganisationTypeGroup(string currentUser, string user, string appID, string overrideID, ApplicationOrganisationTypeGroupDC dc, IRepository<ApplicationOrganisationTypeGroup> dataRepository, IUnitOfWork uow,IExceptionManager exceptionManager, IMappingService mappingService)
            {
                try
                {
    				#region Parameter validation
    
    				// Validate parameters
    				if (string.IsNullOrEmpty(currentUser)) throw new ArgumentOutOfRangeException("currentUser");
    				if (string.IsNullOrEmpty(user)) throw new ArgumentOutOfRangeException("user");
    				if (string.IsNullOrEmpty(appID)) throw new ArgumentOutOfRangeException("appID");
    				if (null == dc) throw new ArgumentOutOfRangeException("dc");
    				if (null == dataRepository) throw new ArgumentOutOfRangeException("dataRepository");
    				if (null == uow) throw new ArgumentOutOfRangeException("uow");
    				if (null == exceptionManager) throw new ArgumentOutOfRangeException("exceptionManager");
    				if (null == mappingService) throw new ArgumentOutOfRangeException("mappingService");
    				
    				#endregion
    
                    using (uow)
                    {
    					// Create a new ID for the ApplicationOrganisationTypeGroup item
    					dc.Code = Guid.NewGuid();
    					
    					// Map data contract to model
                        ApplicationOrganisationTypeGroup destination = mappingService.Map<ApplicationOrganisationTypeGroupDC, ApplicationOrganisationTypeGroup>(dc);
    
    					// Add the new item
                        dataRepository.Add(destination);
    
    					// Commit unit of work
                        uow.Commit();
    					
    					// Map model back to data contract to return new row id.
    					dc = mappingService.Map<ApplicationOrganisationTypeGroup, ApplicationOrganisationTypeGroupDC>(destination);
                    }
    				
    				// Create aggregate data contract
    				ApplicationOrganisationTypeGroupVMDC returnObject = new ApplicationOrganisationTypeGroupVMDC();
    				
    				// Add new item to aggregate
    				returnObject.ApplicationOrganisationTypeGroupItem = dc;
    				
    				return returnObject;
                }
                catch (Exception e)
                {
                    //Prevent exception from propogating across the service interface
                    exceptionManager.ShieldException(e);
    				
    				return null;
                }
            }
    
            #endregion
    
    		#region Update
    
    		/// <summary>
            /// Update a ApplicationOrganisationTypeGroup
            /// </summary>
            /// <param name="currentUser"></param>
            /// <param name="user"></param>
            /// <param name="appID"></param>
            /// <param name="overrideID"></param>
            /// <param name="dc"></param>
            public ApplicationOrganisationTypeGroupVMDC UpdateApplicationOrganisationTypeGroup(string currentUser, string user, string appID, string overrideID, ApplicationOrganisationTypeGroupDC dc)
            {
    			// Create unit of work
                IUnitOfWork uow = new UnitOfWork(currentUser);
    			
    			// Create repository
                IRepository<ApplicationOrganisationTypeGroup> dataRepository = new Repository<ApplicationOrganisationTypeGroup>(uow.ObjectContext, currentUser, user, appID, overrideID);
    
    			//Create ExceptionManager
    			IExceptionManager exceptionManager = new ExceptionManager();
    			
    			//Create MappingService
    			IMappingService mappingService = new MappingService();
    			
    			// Call overload with injected objects
                return UpdateApplicationOrganisationTypeGroup(currentUser, user, appID, overrideID, dc, dataRepository, uow,exceptionManager, mappingService);
            }
    
    		/// <summary>
            /// Update a ApplicationOrganisationTypeGroup
            /// </summary>
            /// <param name="currentUser"></param>
            /// <param name="user"></param>
            /// <param name="appID"></param>
            /// <param name="overrideID"></param>
            /// <param name="dc"></param>
            /// <param name="dataRepository"></param>
            /// <param name="uow"></param>
            public ApplicationOrganisationTypeGroupVMDC UpdateApplicationOrganisationTypeGroup(string currentUser, string user, string appID, string overrideID, ApplicationOrganisationTypeGroupDC dc, IRepository<ApplicationOrganisationTypeGroup> dataRepository, IUnitOfWork uow,IExceptionManager exceptionManager, IMappingService mappingService)
            {
                try
                {
    				#region Parameter validation
    
    				// Validate parameters
    				if (string.IsNullOrEmpty(currentUser)) throw new ArgumentOutOfRangeException("currentUser");
    				if (string.IsNullOrEmpty(user)) throw new ArgumentOutOfRangeException("user");
    				if (string.IsNullOrEmpty(appID)) throw new ArgumentOutOfRangeException("appID");
    				if (null == dc) throw new ArgumentOutOfRangeException("dc");
    				if (null == dataRepository) throw new ArgumentOutOfRangeException("dataRepository");
    				if (null == uow) throw new ArgumentOutOfRangeException("uow");
    				if (null == exceptionManager) throw new ArgumentOutOfRangeException("exceptionManager");
    				if (null == mappingService) throw new ArgumentOutOfRangeException("mappingService");
    				
    				#endregion
    
                    using (uow)
                    {
    					// Map data contract to model
                        ApplicationOrganisationTypeGroup destination = mappingService.Map<ApplicationOrganisationTypeGroupDC, ApplicationOrganisationTypeGroup>(dc);
    
    					// Add the new item
                        dataRepository.Update(destination);
    
    					// Commit unit of work
                        uow.Commit();
    					
    					// Map model back to data contract to return new row id.
    					dc = mappingService.Map<ApplicationOrganisationTypeGroup, ApplicationOrganisationTypeGroupDC>(destination);
                    }
    				
    				// Create new data contract to return
    				ApplicationOrganisationTypeGroupVMDC returnObject = new ApplicationOrganisationTypeGroupVMDC();
    				
    				// Add new item to datacontract
    				returnObject.ApplicationOrganisationTypeGroupItem = dc;
    				
    				// Commit unit of work
    				return returnObject;
    				
                }
                catch (Exception e)
                {
                    //Prevent exception from propogating across the service interface
                    exceptionManager.ShieldException(e);
    				
    				return null;
                }
            }
    
            #endregion
    
    		#region Delete
    
    		/// <summary>
            /// Delete a ApplicationOrganisationTypeGroup
            /// </summary>
            /// <param name="currentUser"></param>
            /// <param name="user"></param>
            /// <param name="appID"></param>
            /// <param name="overrideID"></param>
            /// <param name="code"></param>
            /// <param name="lockID"></param>
            public void DeleteApplicationOrganisationTypeGroup(string currentUser, string user, string appID, string overrideID, string code, string lockID)
            {
    			// Create unit of work
                IUnitOfWork uow = new UnitOfWork(currentUser);
    
    			// Create repository
                IRepository<ApplicationOrganisationTypeGroup> dataRepository = new Repository<ApplicationOrganisationTypeGroup>(uow.ObjectContext, currentUser, user, appID, overrideID);
    			
    			//Create ExceptionManager
    			IExceptionManager exceptionManager = new ExceptionManager();
    			
    			//Create MappingService
    			IMappingService mappingService = new MappingService();
    			
    			// Call overload with injected objects
                DeleteApplicationOrganisationTypeGroup(currentUser, user, appID, overrideID, code, lockID, dataRepository, uow,exceptionManager, mappingService);
            }
    
    		/// <summary>
            /// Update a ApplicationOrganisationTypeGroup
            /// </summary>
            /// <param name="currentUser"></param>
            /// <param name="user"></param>
            /// <param name="appID"></param>
            /// <param name="overrideID"></param>
            /// <param name="code"></param>
            /// <param name="lockID"></param>
            /// <param name="dataRepository"></param>
            /// <param name="uow"></param>
            public void DeleteApplicationOrganisationTypeGroup(string currentUser, string user, string appID, string overrideID, string code, string lockID, IRepository<ApplicationOrganisationTypeGroup> dataRepository, IUnitOfWork uow,IExceptionManager exceptionManager, IMappingService mappingService)
            {
                try
                {
    				#region Parameter validation
    
    				// Validate parameters
    				if (string.IsNullOrEmpty(currentUser)) throw new ArgumentOutOfRangeException("currentUser");
    				if (string.IsNullOrEmpty(user)) throw new ArgumentOutOfRangeException("user");
    				if (string.IsNullOrEmpty(appID)) throw new ArgumentOutOfRangeException("appID");
    				if (string.IsNullOrEmpty(code)) throw new ArgumentOutOfRangeException("code");
    				if (string.IsNullOrEmpty(lockID)) throw new ArgumentOutOfRangeException("lockID");
    				if (null == dataRepository) throw new ArgumentOutOfRangeException("dataRepository");
    				if (null == uow) throw new ArgumentOutOfRangeException("uow");
    				if (null == exceptionManager) throw new ArgumentOutOfRangeException("exceptionManager");
    				if (null == mappingService) throw new ArgumentOutOfRangeException("mappingService");
    				
    				#endregion
    
                    using (uow)
                    {
    					// Convert string to guid
                        Guid codeGuid = Guid.Parse(code);	
    					
    					// Find item based on ID
                        ApplicationOrganisationTypeGroup dataEntity = dataRepository.Single(x => x.Code == codeGuid);
    					
    					// Delete the item
                        dataRepository.Delete(dataEntity);
    
    					// Commit unit of work
                        uow.Commit();
                    }
                }
                catch (Exception e)
                {
                    //Prevent exception from propogating across the service interface
                    exceptionManager.ShieldException(e);
                }
            }
    
            #endregion
    
    		#region SearchApplicationOrganisationTypeGroup
    
    		/// <summary>
            /// Search for ApplicationOrganisationTypeGroup items
            /// </summary>
            /// <param name="currentUser"></param>
            /// <param name="user"></param>
            /// <param name="appID"></param>
            /// <param name="overrideID"></param>
            /// <param name="searchCriteria"></param>
            /// <param name="page"></param>
            /// <param name="pageSize"></param>
            /// <param name="includeInActive"></param>
            /// <returns></returns>
    		public ApplicationOrganisationTypeGroupSearchVMDC SearchApplicationOrganisationTypeGroup(string currentUser, string user, string appID, string overrideID, ApplicationOrganisationTypeGroupSearchCriteriaDC searchCriteria, int page, int pageSize, bool includeInActive)
            {
    			// Create unit of work
    		    IUnitOfWork uow = new UnitOfWork(currentUser);
    
    			// Create repository
                IRepository<ApplicationOrganisationTypeGroup> dataRepository = new Repository<ApplicationOrganisationTypeGroup>(uow.ObjectContext, currentUser, user, appID, overrideID);
    			
    			// Create specification for filtering
    			ISpecification<ApplicationOrganisationTypeGroup> specification = new Specification<ApplicationOrganisationTypeGroup>();
    
    		    // Create isActive specification
                ISpecification<ApplicationOrganisationTypeGroup> isActiveSpecification = new IsActiveSpecification<ApplicationOrganisationTypeGroup>();
    			
    			//Create ExceptionManager
    			IExceptionManager exceptionManager = new ExceptionManager();
    			
    			//Create MappingService
    			IMappingService mappingService = new MappingService();
    			
    			// Call overload with injected objects
                return SearchApplicationOrganisationTypeGroup(currentUser, user, appID, overrideID, searchCriteria, page, pageSize, includeInActive, specification, isActiveSpecification, dataRepository, uow,exceptionManager, mappingService);
    		}
    
    		/// <summary>
            /// Search for ApplicationOrganisationTypeGroup items
            /// </summary>
            /// <param name="currentUser"></param>
            /// <param name="user"></param>
            /// <param name="appID"></param>
            /// <param name="overrideID"></param>
            /// <param name="searchCriteria"></param>
            /// <param name="page"></param>
            /// <param name="pageSize"></param>
            /// <param name="includeInActive"></param>
    		/// <param name="specification"></param>
            /// <param name="dataRepository"></param>
            /// <param name="uow"></param>
            /// <returns></returns>
    		public ApplicationOrganisationTypeGroupSearchVMDC SearchApplicationOrganisationTypeGroup(string currentUser, string user, string appID, string overrideID, ApplicationOrganisationTypeGroupSearchCriteriaDC searchCriteria, int page, int pageSize, bool includeInActive,
    		ISpecification<ApplicationOrganisationTypeGroup> specification, ISpecification<ApplicationOrganisationTypeGroup> isActiveSpecification, IRepository<ApplicationOrganisationTypeGroup> dataRepository, IUnitOfWork uow,IExceptionManager exceptionManager, IMappingService mappingService)
            {
    		    try
                {
    				#region Parameter validation
    
    				// Validate parameters
    				if (string.IsNullOrEmpty(currentUser)) throw new ArgumentOutOfRangeException("currentUser");
    				if (string.IsNullOrEmpty(user)) throw new ArgumentOutOfRangeException("user");
    				if (string.IsNullOrEmpty(appID)) throw new ArgumentOutOfRangeException("appID");
    				if (null == dataRepository) throw new ArgumentOutOfRangeException("dataRepository");
    				if (null == specification) throw new ArgumentOutOfRangeException("specification");
    				if (null == uow) throw new ArgumentOutOfRangeException("uow");
    				if (null == exceptionManager) throw new ArgumentOutOfRangeException("exceptionManager");
    				if (null == mappingService) throw new ArgumentOutOfRangeException("mappingService");
    				
    				#endregion
    
                    using (uow)
                    {
    		    // Evaluate search criteria if supplied
                        if (null != searchCriteria)
                        {
                            EvaluateApplicationOrganisationTypeGroupSearchCriteria(searchCriteria, ref specification);
                        }
    
                        if (!includeInActive)
                        {
                            specification = specification.And(isActiveSpecification);
                        }
    
    					// Set default sort expression
    					System.Linq.Expressions.Expression<Func<ApplicationOrganisationTypeGroup, Object>> sortExpression = x => new { x.IsActive };
    					
    				    // Find all items that satisfy the specification created above.
                        IEnumerable<ApplicationOrganisationTypeGroup> dataEntities = dataRepository.Find(specification, sortExpression, page, pageSize);
    					
    					// Get total count of items for search critera
    					int itemCount = dataRepository.Count(specification);
    
    					ApplicationOrganisationTypeGroupSearchVMDC results = new ApplicationOrganisationTypeGroupSearchVMDC();
    
    					// Convert to data contracts
                        List<ApplicationOrganisationTypeGroupSearchMatchDC> destinations = mappingService.Map<IEnumerable<ApplicationOrganisationTypeGroup>, List<ApplicationOrganisationTypeGroupSearchMatchDC>>(dataEntities);
    
    					results.MatchList = destinations;
    					results.SearchCriteria = searchCriteria;
    					results.RecordCount = itemCount;
    
                        return results;
                    }
                }
                catch (Exception e)
                {
                    //Prevent exception from propogating across the service interface
                    exceptionManager.ShieldException(e);
    
                    return null;
                }
    		}
    		
    		// Partial method for evaluation of ApplicationOrganisationTypeGroup search criteria
            partial void EvaluateApplicationOrganisationTypeGroupSearchCriteria(ApplicationOrganisationTypeGroupSearchCriteriaDC searchCriteria, ref ISpecification<ApplicationOrganisationTypeGroup> specification);
    		
    		#endregion
    		
    		#region GetAllApplicationOrganisationTypeGroup
    		/// <summary>
            /// 
            /// </summary>
            /// <param name="currentUser"></param>
            /// <param name="user"></param>
            /// <param name="appID"></param>
            /// <param name="overrideID"></param>
    		/// <param name="includeInActive"></param>
            /// <returns></returns>
            public List<ApplicationOrganisationTypeGroupDC> GetAllApplicationOrganisationTypeGroup(string currentUser, string user, string appID, string overrideID, bool includeInActive)
            {
    			// Create unit of work
                IUnitOfWork uow = new UnitOfWork(currentUser);
    
    			// Create repository
                Repository<ApplicationOrganisationTypeGroup> dataRepository = new Repository<ApplicationOrganisationTypeGroup>(uow.ObjectContext, currentUser, user, appID, overrideID);
    			
    			// Create specification for filtering
    			ISpecification<ApplicationOrganisationTypeGroup> specification = new Specification<ApplicationOrganisationTypeGroup>();
    			
    			// Create isActive specification
                ISpecification<ApplicationOrganisationTypeGroup> isActiveSpecification =  new IsActiveSpecification<ApplicationOrganisationTypeGroup>();
    
    			//Create ExceptionManager
    			IExceptionManager exceptionManager = new ExceptionManager();
    			
    			//Create MappingService
    			IMappingService mappingService = new MappingService();
    			
    			// Call overload with injected objects
                return GetAllApplicationOrganisationTypeGroup(currentUser, user, appID, overrideID, includeInActive, specification, isActiveSpecification, dataRepository, uow, exceptionManager, mappingService);
            }
    
    		/// <summary>
            /// 
            /// </summary>
            /// <param name="currentUser"></param>
            /// <param name="user"></param>
            /// <param name="appID"></param>
            /// <param name="overrideID"></param>
    		/// <param name="includeInActive"></param>
    		/// <param name="specification"></param>
            /// <param name="dataRepository"></param>
            /// <param name="uow"></param>
            /// <returns></returns>
            public List<ApplicationOrganisationTypeGroupDC> GetAllApplicationOrganisationTypeGroup(string currentUser, string user, string appID, string overrideID,  bool includeInActive, ISpecification<ApplicationOrganisationTypeGroup> specification, ISpecification<ApplicationOrganisationTypeGroup> isActiveSpecification, IRepository<ApplicationOrganisationTypeGroup> dataRepository, IUnitOfWork uow,IExceptionManager exceptionManager, IMappingService mappingService)
            {
                try
                {
    				#region Parameter validation
    
    				// Validate parameters
    				if (string.IsNullOrEmpty(currentUser)) throw new ArgumentOutOfRangeException("currentUser");
    				if (string.IsNullOrEmpty(user)) throw new ArgumentOutOfRangeException("user");
    				if (string.IsNullOrEmpty(appID)) throw new ArgumentOutOfRangeException("appID");
    				if (null == dataRepository) throw new ArgumentOutOfRangeException("dataRepository");
    				if (null == specification) throw new ArgumentOutOfRangeException("specification");
    				if (null == uow) throw new ArgumentOutOfRangeException("uow");
    				if (null == exceptionManager) throw new ArgumentOutOfRangeException("exceptionManager");
    				if (null == mappingService) throw new ArgumentOutOfRangeException("mappingService");
    				
    				#endregion
    
                    using (uow)
                    {
    					// Build specification
                        if (!includeInActive)
                        {
                            specification = specification.And(isActiveSpecification);
                        }
    
    					// Set default sort expression
    					System.Linq.Expressions.Expression<Func<ApplicationOrganisationTypeGroup, Object>> sortExpression = x => new { x.IsActive };
    					
                        // Find all items that satisfy the specification created above.
                        IEnumerable<ApplicationOrganisationTypeGroup> dataEntities = dataRepository.Find(specification, sortExpression);
    					
    					// Convert to data contracts
                        List<ApplicationOrganisationTypeGroupDC> destinations = mappingService.Map<IEnumerable<ApplicationOrganisationTypeGroup>, List<ApplicationOrganisationTypeGroupDC>>(dataEntities);
    
                        return destinations;
                    }
                }
                catch (Exception e)
                {
                    //Prevent exception from propogating across the service interface
                    exceptionManager.ShieldException(e);
    
                    return null;
                }
            }
    		#endregion
    
            #region GetApplicationOrganisationTypeGroup
    	
    		/// <summary>
        	/// Retrieve a ApplicationOrganisationTypeGroup with associated lookups
        	/// </summary>
        	/// <param name="currentUser"></param>
        	/// <param name="user"></param>
        	/// <param name="appID"></param>
        	/// <param name="overrideID"></param>
        	/// <param name="code"></param>
        	/// <returns></returns>
            public ApplicationOrganisationTypeGroupVMDC GetApplicationOrganisationTypeGroup(string currentUser, string user, string appID, string overrideID, string code)
            {
    			//Create ExceptionManager
    			IExceptionManager exceptionManager = new ExceptionManager();
    			
    			//Create MappingService
    			IMappingService mappingService = new MappingService();
    			
    			// Create unit of work
                IUnitOfWork uow = new UnitOfWork(currentUser);
    
    			// Create repository
                IRepository<ApplicationOrganisationTypeGroup> dataRepository = new Repository<ApplicationOrganisationTypeGroup>(uow.ObjectContext, currentUser, user, appID, overrideID);
    
    			// Create repositories for lookup data
    			IRepository<Application> applicationRepository = new Repository<Application>(uow.ObjectContext, currentUser, user, appID, overrideID);
    			IRepository<OrganisationTypeGroup> organisationTypeGroupRepository = new Repository<OrganisationTypeGroup>(uow.ObjectContext, currentUser, user, appID, overrideID);
    			IRepository<Organisation> rootOrganisationForApplicationRepository = new Repository<Organisation>(uow.ObjectContext, currentUser, user, appID, overrideID);
    
    			// Call overload with injected objects
                return GetApplicationOrganisationTypeGroup(currentUser, user, appID, overrideID, code, uow, dataRepository
    			, applicationRepository
    			, organisationTypeGroupRepository
    			, rootOrganisationForApplicationRepository
    			, exceptionManager, mappingService);
            }
    
    		/// <summary>
            /// Retrieve a ApplicationOrganisationTypeGroup with associated lookups
            /// </summary>
            /// <param name="currentUser"></param>
            /// <param name="user"></param>
            /// <param name="appID"></param>
            /// <param name="overrideID"></param>
            /// <param name="code"></param>
            /// <param name="dataRepository"></param>
            /// <param name="uow"></param>
            /// <returns></returns>
            public ApplicationOrganisationTypeGroupVMDC GetApplicationOrganisationTypeGroup(string currentUser, string user, string appID, string overrideID, string code, IUnitOfWork uow, IRepository<ApplicationOrganisationTypeGroup> dataRepository
    			,IRepository<Application> applicationRepository
    			,IRepository<OrganisationTypeGroup> organisationTypeGroupRepository
    			,IRepository<Organisation> rootOrganisationForApplicationRepository
    			, IExceptionManager exceptionManager, IMappingService mappingService)
    		
            {
                try
                {
    				#region Parameter validation
    
    				// Validate parameters
    				if (string.IsNullOrEmpty(currentUser)) throw new ArgumentOutOfRangeException("currentUser");
    				if (string.IsNullOrEmpty(user)) throw new ArgumentOutOfRangeException("user");
    				if (string.IsNullOrEmpty(appID)) throw new ArgumentOutOfRangeException("appID");
    				if (null == dataRepository) throw new ArgumentOutOfRangeException("dataRepository");
    				if (null == uow) throw new ArgumentOutOfRangeException("uow");
    				if (null == exceptionManager) throw new ArgumentOutOfRangeException("exceptionManager");
    				if (null == mappingService) throw new ArgumentOutOfRangeException("mappingService");
    				
    				#endregion
    
                    using (uow)
                    {
    				
    					ApplicationOrganisationTypeGroupDC destination = null;
    					
    					// If code is null then just return supporting lists
    					if (!string.IsNullOrEmpty(code))
    					{
    						// Convert code to Guid
    	                    Guid codeGuid = Guid.Parse(code);
    
    						// Retrieve specific ApplicationOrganisationTypeGroup
    	                    ApplicationOrganisationTypeGroup dataEntity = dataRepository.Single(x => x.Code == codeGuid);
    
    						// Convert to data contract for passing through service interface
    	                    destination = mappingService.Map<ApplicationOrganisationTypeGroup, ApplicationOrganisationTypeGroupDC>(dataEntity);
    					}
    
    					IEnumerable<Application> applicationList = applicationRepository.GetAll(x => new {x.Description});
    					IEnumerable<OrganisationTypeGroup> organisationTypeGroupList = organisationTypeGroupRepository.GetAll(x => x.Name);
    					IEnumerable<Organisation> rootOrganisationForApplicationList = rootOrganisationForApplicationRepository.GetAll(x => x.Name);
    
    					List<ApplicationDC> applicationDestinationList = mappingService.Map<List<ApplicationDC>>(applicationList);
    					List<OrganisationTypeGroupDC> organisationTypeGroupDestinationList = mappingService.Map<List<OrganisationTypeGroupDC>>(organisationTypeGroupList);
    					List<OrganisationDC> rootOrganisationForApplicationDestinationList = mappingService.Map<List<OrganisationDC>>(rootOrganisationForApplicationList);
    
        				// Create aggregate contract
                        ApplicationOrganisationTypeGroupVMDC returnObject = new ApplicationOrganisationTypeGroupVMDC();
    
                        returnObject.ApplicationOrganisationTypeGroupItem = destination;
    					returnObject.ApplicationList = applicationDestinationList;
    					returnObject.OrganisationTypeGroupList = organisationTypeGroupDestinationList;
    					returnObject.RootOrganisationForApplicationList = rootOrganisationForApplicationDestinationList;
                        
    					return returnObject;
                    }
                }
                catch (Exception e)
                {
                    //Prevent exception from propogating across the service interface
                    exceptionManager.ShieldException(e);
    
                    return null;
                }
            }
    	
    		#endregion	
    
    	

            #endregion
    }

        #endregion
}
